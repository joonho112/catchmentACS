% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rings.R
\name{cacs_rings_to_cumulative}
\alias{cacs_rings_to_cumulative}
\title{Combine drive-time bands into cumulative drive-time areas}
\usage{
cacs_rings_to_cumulative(iso_sf, drive_times = NULL)
}
\arguments{
\item{iso_sf}{An \code{sf} object of polygons in EPSG:4326 (longitude and
latitude), with one row for each site and band and a \code{site_id} column of
strings. The columns \code{isomin} and \code{isomax} give the drive times, in
whole minutes, at which each band starts and ends: \code{isomin} must be 0 or
more and \code{isomax} above \code{isomin}. A \code{ring_topology} column, if
present, must hold only \code{"annulus"} (bands) or \code{"cumulative"}; it does
not change the result.}

\item{drive_times}{A numeric vector of drive times in minutes, each one of
the \code{isomax} values, or \code{NULL} (the default) for all of them. Every site
must have a band that ends at each of these drive times.}
}
\value{
An \code{sf} object with one row for each site and drive time, sorted by
\code{site_id} and then by drive time. The geometry, in a column named
\code{geometry}, is the union of the bands up to that drive time. The other
columns are copied from the band that ends at that drive time, except
four: \code{isomin} is set to 0, \code{isomax} and \code{drive_time_min} to the drive
time, and \code{ring_topology} to \code{"cumulative"}. \code{\link[=cacs_intersect_weight]{cacs_intersect_weight()}}
and \code{\link[=cacs_run]{cacs_run()}} also need the other columns of a \code{\link[=cacs_isochrone]{cacs_isochrone()}}
result; \code{\link[=cacs_validate_iso]{cacs_validate_iso()}} lists any that are missing.
}
\description{
Some routing tools return drive-time bands, also called rings: for each
site, the area reachable in 0 to 5 minutes, the area reachable in 5 to 10
minutes, and so on. The package uses cumulative areas instead, in which
the 10-minute area covers everything reachable within 10 minutes and so
contains the 5-minute area. This function makes them by joining each band
with the bands of the shorter drive times (their geometric union).
}
\details{
For each site and drive time, the bands that end at or before that drive
time are joined. If all of them start at 0, the rows are taken to be
cumulative areas already; otherwise they must run from 0 to that drive
time without a gap in minutes, or the function gives an error. The
geometries are not compared, so bands that overlap or leave gaps between
them are joined as they are.
}
\examples{
# Two bands for one site: 0 to 5 minutes and 5 to 10 minutes
poly <- function(xmin, xmax, ymin, ymax) {
  sf::st_polygon(list(rbind(
    c(xmin, ymin),
    c(xmax, ymin),
    c(xmax, ymax),
    c(xmin, ymax),
    c(xmin, ymin)
  )))
}

bands <- sf::st_sf(
  site_id = c("A", "A"),
  isomin = c(0L, 5L),
  isomax = c(5L, 10L),
  geometry = sf::st_sfc(
    poly(0, 1, 0, 1),
    poly(1, 2, 0, 1),
    crs = 4326
  )
)

# The 10-minute area is the union of the two bands
cumulative <- cacs_rings_to_cumulative(bands, drive_times = c(5, 10))
sf::st_drop_geometry(cumulative)[
  , c("site_id", "isomin", "isomax", "drive_time_min", "ring_topology")
]
unique(cumulative$ring_topology)

}
\seealso{
Other rates and margins of error:
\code{\link{cacs_acs_default_rates}},
\code{\link[=cacs_moe_to_se]{cacs_moe_to_se()}},
\code{\link[=cacs_se_to_moe]{cacs_se_to_moe()}}
}
\concept{rates and margins of error}
