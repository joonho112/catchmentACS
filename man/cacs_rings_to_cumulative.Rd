% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rings.R
\name{cacs_rings_to_cumulative}
\alias{cacs_rings_to_cumulative}
\title{Convert annulus-topology isochrone rings to cumulative-topology rings}
\usage{
cacs_rings_to_cumulative(iso_sf, drive_times = NULL)
}
\arguments{
\item{iso_sf}{An \code{sf} data frame with polygon geometry and columns
\code{site_id}, \code{isomin}, and \code{isomax}.}

\item{drive_times}{Optional numeric vector of drive times to retain. If
\code{NULL}, all unique \code{isomax} values found in \code{iso_sf} are retained.}
}
\value{
An \code{sf} object with one row per \code{site_id} by requested drive time,
\code{isomin = 0}, \code{isomax = drive_time_min}, and
\code{ring_topology = "cumulative"}.
}
\description{
The current release accepts cumulative isochrone rings only: each row
represents the area reachable within \verb{[0, isomax]}. If your isochrone source
returns annulus bands such as \verb{(0, 5]}, \verb{(5, 10]}, and \verb{(10, 15]}, use this
helper to union the bands into cumulative polygons before calling
\code{\link[=cacs_intersect_weight]{cacs_intersect_weight()}} or passing precomputed isochrones to \code{\link[=cacs_run]{cacs_run()}}.
}
\examples{
poly <- function(xmin, xmax, ymin, ymax) {
  sf::st_polygon(list(rbind(
    c(xmin, ymin),
    c(xmax, ymin),
    c(xmax, ymax),
    c(xmin, ymax),
    c(xmin, ymin)
  )))
}

annulus <- sf::st_sf(
  site_id = c("A", "A"),
  isomin = c(0L, 5L),
  isomax = c(5L, 10L),
  geometry = sf::st_sfc(
    poly(0, 1, 0, 1),
    poly(1, 2, 0, 1),
    crs = 4326
  )
)

cumulative <- cacs_rings_to_cumulative(annulus, drive_times = c(5, 10))
sf::st_drop_geometry(cumulative)[
  , c("site_id", "isomin", "isomax", "drive_time_min", "ring_topology")
]
unique(cumulative$ring_topology)

}
\seealso{
\code{\link[=cacs_validate_iso]{cacs_validate_iso()}}, \code{\link[=cacs_intersect_weight]{cacs_intersect_weight()}}, \code{\link[=cacs_run]{cacs_run()}},
\code{\link[=cacs_isochrone]{cacs_isochrone()}}

Other rate and MOE helpers:
\code{\link{cacs_acs_default_rates}},
\code{\link[=cacs_moe_to_se]{cacs_moe_to_se()}},
\code{\link[=cacs_se_to_moe]{cacs_se_to_moe()}}
}
\concept{rate and MOE helpers}
