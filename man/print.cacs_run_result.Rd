% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run.R
\name{print.cacs_run_result}
\alias{print.cacs_run_result}
\title{Print the result of \code{cacs_run()}}
\usage{
\method{print}{cacs_run_result}(x, ..., n_head = 5, n_tail = 3)
}
\arguments{
\item{x}{A \code{cacs_run_result} object, as returned by \code{\link[=cacs_run]{cacs_run()}}.}

\item{...}{Passed to the print method for tibbles (\code{\link[tibble:print.tbl]{tibble::print.tbl()}})
for the preview, such as \code{n}, the number of rows to show.}

\item{n_head, n_tail}{Single numbers that have no effect (\code{n} in \code{...}
sets the number of rows).}
}
\value{
\code{x}, invisibly.
}
\description{
Prints a result of \code{\link[=cacs_run]{cacs_run()}} as a header about the run, two tables of
the five rates, and a preview of the rows.
}
\details{
Each part of the output starts with a title line:
\describe{
\item{catchmentACS run result}{The time the result was created and the
version of catchmentACS that created it, the routing service and the
drive times given to \code{\link[=cacs_run]{cacs_run()}}, and the routing profile of the
drive-time areas. The drive times are the ones given in the call, so
when the areas come from \code{precomputed_isochrones} the line can name a
drive time that no row of the result has. The line "Sites" gives the
number of sites with at
least one drive-time area (\code{success}), the number of sites given to
\code{\link[=cacs_run]{cacs_run()}} in \code{sites} (\code{total}), and \code{total} minus \code{success}, but
not below 0 (\code{failed}). If \code{precomputed_isochrones} has areas for
sites that are not in \code{sites}, \code{success} counts them too, so it can
exceed \code{total} and \code{failed} can miss sites without a drive-time area.
When tracts were skipped because they have no area, one more line
gives the length of the \code{skipped_geoids} attribute, which lists each
such tract once for each of its variables.}
\item{Top 5 rates (cross-site mean +/- sd)}{One row for each of the
five rates, highest mean first. The columns are the unweighted mean
(\code{mean_estimate}) and the standard deviation (\code{sd_estimate}) of the
rate over all site and drive-time pairs, leaving out missing values,
and \code{n}, the number of pairs, including those where the rate is
missing.}
\item{Rates per site}{The estimates of the five rates, with one row for
each site and drive time. The table is shown when the result has at
most \code{getOption("catchmentACS.summary_per_site_max")} sites (12 by
default). With \code{0} it is never shown, and with \code{Inf} always.}
\item{Tibble preview}{The rows, printed as a tibble. Before this title,
the line "Wall clock" gives the time that \code{\link[=cacs_run]{cacs_run()}} took, in
seconds. The last line names \code{as_tibble(x)}, which returns a plain
tibble (see \code{\link[=as_tibble.cacs_run_result]{as_tibble.cacs_run_result()}}), and \code{x[]}, which is the
same object and prints in the same way.}
}

The rate tables need the columns \code{variable}, \code{estimate}, and \code{moe}, so
they are not shown for the list-column form of the result. Subsets made
with \code{[} or with dplyr verbs such as \code{\link[dplyr:filter]{dplyr::filter()}}, and tables made
with \code{\link[dplyr:count]{dplyr::count()}} or \code{\link[dplyr:distinct]{dplyr::distinct()}}, keep the class and are
printed in the same way, with the header of the whole result. Printing
one that has those three columns but no \code{site_id} column gives an error.
Without the \code{cacs_run_result_metadata} attribute, the result is printed as
a plain tibble.
}
\section{Capturing the output}{

The tables are ordinary printed output, and every other line is an R
message written with the cli package. These calls keep different parts:

\if{html}{\out{<div class="sourceCode">}}\preformatted{capture.output(print(x))                    # the tables
capture.output(print(x), type = "message")  # the cli output
testthat::capture_messages(print(x))        # the cli output, in tests
suppressMessages(print(x))                  # prints only the tables
}\if{html}{\out{</div>}}
}

\examples{
# A bundled cacs_run() result for a 10-minute area in Birmingham, Alabama
out <- readRDS(system.file("extdata", "visual_walkthrough_fixture.rds",
                           package = "catchmentACS"))$run_result

# One site and one drive time, so the standard deviations are NA
print(out, n = 5)
}
\seealso{
Other result summaries:
\code{\link[=as_tibble.cacs_run_result]{as_tibble.cacs_run_result()}},
\code{\link[=cacs_describe]{cacs_describe()}},
\code{\link[=cacs_summary_as_markdown]{cacs_summary_as_markdown()}},
\code{\link[=group_by.cacs_run_result]{group_by.cacs_run_result()}},
\code{\link[=print.cacs_run_summary]{print.cacs_run_summary()}},
\code{\link[=summary.cacs_run_result]{summary.cacs_run_result()}}
}
\concept{result summaries}
