% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-site.R
\name{cacs_plot_site_rates}
\alias{cacs_plot_site_rates}
\title{Map a site's headline rate estimates (walkthrough stage 4)}
\usage{
cacs_plot_site_rates(
  site_id = NULL,
  lat = NULL,
  lon = NULL,
  site_name = NULL,
  iso_sf,
  run_result,
  sites_df = NULL,
  tiles = "CartoDB.Positron",
  padding_km = 5,
  ...
)
}
\arguments{
\item{site_id}{Site identifier to look up in \code{sites_df}. When omitted,
\code{sites_df} defaults to the bundled \code{cacs_alabama_sites} table.}

\item{lat, lon}{Alternative to \code{site_id}: a direct latitude/longitude
coordinate pair in WGS84 (\code{lat} in \verb{[-90, 90]}, \code{lon} in \verb{[-180, 180]}),
matched to the nearest known site (see the section above).}

\item{site_name}{Alternative to \code{site_id}: an exact match against the
\code{site_name} column of \code{sites_df}.}

\item{iso_sf}{Isochrone polygons (an \code{sf} object) such as those returned
by \code{\link[=cacs_isochrone]{cacs_isochrone()}}.}

\item{run_result}{Output of \code{\link[=cacs_run]{cacs_run()}}; filtered internally to the
chosen site's rate rows.}

\item{sites_df}{An \code{sf} table of sites. When \code{NULL}, the bundled
\code{cacs_alabama_sites} table is used.}

\item{tiles}{Name of the \code{leaflet} basemap provider tiles
(default \code{"CartoDB.Positron"}).}

\item{padding_km}{Positive number giving the map padding, in kilometres,
around the site point when setting the initial view. Default \code{5}.}

\item{...}{Reserved for future keyword arguments; currently unused.}
}
\value{
A \code{leaflet} map (an HTML widget).
}
\description{
Draws the "answer" map for a site: a site marker on the combined
outline of all its isochrone rings, with a pinned pop-up listing the
five headline rates (poverty, SNAP, SSI, unemployment, and labour-force
participation). Each rate shows its point estimate, its 90 percent
margin of error (MOE) band, and the tract counts used in the numerator
and denominator (the \code{n_tracts_num} and \code{n_tracts_den} columns produced
by \code{\link[=cacs_intersect_weight]{cacs_intersect_weight()}}). This is the fourth and final stage map.

When a rate's margin of error had to be computed with the fallback
formula rather than the primary one (recorded in the \code{moe_fallback}
column), its name is prefixed with an asterisk so you know the reported
margin used the conservative fallback formula.

You select the site with a \code{site_id}, a \code{lat}/\code{lon} pair, or a
\code{site_name}, tried in that order.
}
\section{Resolving the site}{

Because the rate table is keyed by \code{site_id}, this map needs a concrete
site. If you supply a direct \code{lat}/\code{lon} pair, it is matched to the
nearest site in \code{sites_df} whose \code{site_id} also appears in \code{run_result},
and a message reports the matched \code{site_id}, the \code{site_name} if known,
and the distance in kilometres. If the nearest match is more than 5 km
from the requested point, a warning asks you to check the coordinates or
pass a \code{site_id} directly.
}

\examples{
\dontrun{
iso <- readRDS(system.file("extdata", "legacy_2025_isochrones.rds",
                           package = "catchmentACS"))
result <- ... # a cacs_run() output
cacs_plot_site_rates(
  site_id = "AL_SITE_01",
  iso_sf  = iso,
  run_result = result
)
}
}
\seealso{
\code{\link[=cacs_plot_site_pipeline]{cacs_plot_site_pipeline()}} to build all four stage maps at
once, and \code{\link[=cacs_run]{cacs_run()}} for the end-to-end aggregation these maps
illustrate.

Other visual walkthrough helpers:
\code{\link[=cacs_plot_site_intersection]{cacs_plot_site_intersection()}},
\code{\link[=cacs_plot_site_isochrone]{cacs_plot_site_isochrone()}},
\code{\link[=cacs_plot_site_pipeline]{cacs_plot_site_pipeline()}},
\code{\link[=cacs_plot_site_weighted]{cacs_plot_site_weighted()}},
\code{\link[=format.cacs_site_plot_pipeline]{format.cacs_site_plot_pipeline()}},
\code{\link[=print.cacs_site_plot_pipeline]{print.cacs_site_plot_pipeline()}}
}
\concept{visual walkthrough helpers}
