% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/isochrone-osrm.R
\name{cacs_validate_osrm_endpoint}
\alias{cacs_validate_osrm_endpoint}
\title{Check whether an OSRM server answers a routing request}
\usage{
cacs_validate_osrm_endpoint(server = NULL, timeout = 5)
}
\arguments{
\item{server}{A string giving the address of the OSRM server, or \code{NULL}
(the default). With \code{NULL} or \code{""}, the address is the value of the
option \code{osrm.server} at the time of the call, or the public OSRM demo
server \verb{https://routing.openstreetmap.de/} when the option is not set.
Loading the osrm package, for example with \code{library(osrm)}, sets that
option to the demo server and replaces a value set before (see the "OSRM
servers" section of \code{\link[=cacs_isochrone]{cacs_isochrone()}}). The option
\code{catchmentACS.osrm_docker_server} is not used, so the local server that
\code{\link[=cacs_isochrone]{cacs_isochrone()}} uses with
\code{osrm_mode = "docker"} is checked only when its address is given here.}

\item{timeout}{A single positive number giving the number of seconds to
wait for an answer; the default is 5.}
}
\value{
A tibble with one row and four columns:
\describe{
\item{\code{endpoint}}{The server address used.}
\item{\code{quota_ok}}{\code{TRUE} when the server answered with HTTP status
200, and \code{FALSE} otherwise, including when there was no answer.}
\item{\code{response_ms}}{The time taken, in milliseconds, including any
time spent waiting for an answer that did not come.}
\item{\code{http_status}}{The HTTP status of the answer, as an integer, or
\code{NA} when there was no answer.}
}
}
\description{
Sends one small request to an Open Source Routing Machine (OSRM) server
and reports whether the server answered it with HTTP status 200.
\code{\link[=cacs_isochrone]{cacs_isochrone()}} sends many requests for each site, so this shows
beforehand whether the server can be reached and is accepting requests.
}
\details{
When the server answers with status 429, meaning that its limit on
requests has been reached, the function gives a warning of class
\code{catchmentACS_warning_provider_quota_exhausted} and still returns its
result. Other statuses give no warning. A server that cannot be reached,
or does not answer within \code{timeout} seconds, gives \code{quota_ok = FALSE} and
\code{http_status = NA} without a warning or an error.

The request is for a route between two fixed points near Birmingham,
Alabama, so a server whose map data do not cover them may not answer with
status 200. It is sent to \verb{route/v1/driving/-86.8,33.5;-86.7,33.4} under
the server address; on the public demo server, \verb{routed-car/},
\verb{routed-bike/}, or \verb{routed-foot/} comes first, following the option
\code{osrm.profile}.
}
\examples{
# A local port where no server is expected to answer: quota_ok is FALSE
# and http_status is NA, without an error
cacs_validate_osrm_endpoint("http://localhost:9", timeout = 1)

# Contacts the public OSRM demo server, which limits the requests it
# accepts, and then builds a drive-time area there.
\dontrun{
site_07 <- data.frame(site_id = "AL_SITE_07", lon = -85.365, lat = 31.655)

# Check the server before building the drive-time areas
check <- cacs_validate_osrm_endpoint()
check
if (check$quota_ok) {
  iso <- cacs_isochrone(site_07, drive_times = 10)
} else if (identical(check$http_status, 429L)) {
  # Request limit reached: build the areas with a local OSRM server
  iso <- cacs_isochrone(site_07, drive_times = 10, osrm_mode = "docker")
} else {
  stop("The OSRM server is not accepting requests; see check$http_status.")
}
}
}
\seealso{
Other validation and conditions:
\code{\link[=cacs_acs_validate]{cacs_acs_validate()}},
\code{\link[=cacs_capture_conditions]{cacs_capture_conditions()}},
\code{\link[=cacs_validate_iso]{cacs_validate_iso()}},
\code{\link{catchmentACS-conditions}}
}
\concept{validation and conditions}
