% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/isochrone-osrm.R
\name{cacs_validate_osrm_endpoint}
\alias{cacs_validate_osrm_endpoint}
\title{Pre-flight check of an OSRM endpoint's current quota state}
\usage{
cacs_validate_osrm_endpoint(server = NULL, timeout = 5)
}
\arguments{
\item{server}{Character scalar: the OSRM endpoint URL. Defaults to the
current \code{getOption("osrm.server")}, or to the package's built-in
public-demo server if that option is unset.}

\item{timeout}{Numeric scalar: HTTP timeout in seconds (default \code{5}).}
}
\value{
A 1-row tibble with columns:
\itemize{
\item \code{endpoint} (character) — the configured endpoint/base server used to
build the probe URL.
\item \code{quota_ok} (logical) — \code{TRUE} if the probe request returned HTTP 200,
\code{FALSE} on HTTP 429 or any other error.
\item \code{response_ms} (numeric) — wall-clock milliseconds of the probe.
\item \code{http_status} (integer) — HTTP status code, or \code{NA_integer_} on
connection failure.
}
}
\description{
Sends a small, lightweight \verb{/route} ping that tests whether an OSRM routing
endpoint is currently accepting requests, before the much heavier
\code{\link[=cacs_isochrone]{cacs_isochrone()}} table-API path. Use it when you are about to launch a
large batch (for example 50 or more sites at a high \code{res} grid resolution)
and want to fail fast on an unreachable endpoint or an exhausted request
quota. For the public \code{routing.openstreetmap.de} demo server, the probe
expands the configured base server to the profile-prefixed service path
(for example \verb{/routed-car/route/v1/driving} for the default car profile).
Custom or self-hosted OSRM endpoints are probed at \verb{/route/v1/driving}.
}
\details{
On HTTP 429 (rate limit) the function emits a classed warning
(\code{catchmentACS_warning_provider_quota_exhausted}) and returns
\code{quota_ok = FALSE}. Other non-200 HTTP statuses also return
\code{quota_ok = FALSE} but do not imply quota exhaustion. You can then inspect
\code{http_status}, switch to \code{osrm_mode = "docker"}, or call \code{\link[=cacs_isochrone]{cacs_isochrone()}}
directly and rely on its provider error message. This function never throws
an error, even on quota exhaustion: it is a probe, not a gate.
}
\section{Side effects}{

On HTTP 429, emits \code{catchmentACS_warning_provider_quota_exhausted} —
suppressible by handling the class via \code{tryCatch()} or
\code{withCallingHandlers()}. Other HTTP statuses or connection errors do
NOT emit a warning; inspect \code{http_status} and \code{quota_ok}.
}

\examples{
\dontrun{
# Quick quota check before a batch run.
state <- cacs_validate_osrm_endpoint()
if (state$quota_ok) {
  iso <- cacs_isochrone(sites_sf, 10L, "osrm")
} else if (identical(state$http_status, 429L)) {
  message("OSRM demo currently saturated; switching to docker.")
  iso <- cacs_isochrone(sites_sf, 10L, "osrm", osrm_mode = "docker")
} else {
  stop("OSRM probe failed; inspect state$http_status before routing.")
}
}
}
\seealso{
\code{\link[=cacs_isochrone]{cacs_isochrone()}} for the isochrone builder this probe guards,
and \code{\link[=cacs_validate_iso]{cacs_validate_iso()}} for validating an isochrone object's schema.

Other validation and conditions:
\code{\link[=cacs_acs_validate]{cacs_acs_validate()}},
\code{\link[=cacs_capture_conditions]{cacs_capture_conditions()}},
\code{\link[=cacs_validate_iso]{cacs_validate_iso()}},
\code{\link{catchmentACS-conditions}}
}
\concept{validation and conditions}
