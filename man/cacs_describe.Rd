% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/describe.R
\name{cacs_describe}
\alias{cacs_describe}
\alias{print.cacs_description}
\title{Describe how a result was produced}
\usage{
cacs_describe(x, ...)

\method{print}{cacs_description}(x, ...)
}
\arguments{
\item{x}{An object to describe (see Details). For \code{print()}, a list
returned by \code{cacs_describe()}.}

\item{...}{Not used.}
}
\value{
\code{cacs_describe()} returns, invisibly, a list of class
\code{cacs_description} with these elements:
\describe{
\item{\code{object_type}}{A string naming the kind of object (see
Details).}
\item{\code{sections}}{A named list with one character vector of printed
lines for each section.}
\item{\code{data}}{A named list of the attributes that the sections are
drawn from, such as \code{rate_provenance}, the \code{cacs_rate_provenance}
attribute of \code{x} or \code{NULL}. For a result of \code{\link[=cacs_run]{cacs_run()}}, it also
holds \code{cache_state}, from \code{\link[=cacs_get_cache_state]{cacs_get_cache_state()}}.}
\item{\code{attributes_present}}{A character vector of the names of the
package's attributes that \code{x} has.}
}
\code{print()} prints \code{x} in the same way and returns it invisibly.
}
\description{
Prints a description of an object made by the package, drawn from the
attributes that record how it was produced, and returns the description
invisibly as a list. It describes the results of \code{\link[=cacs_run]{cacs_run()}} and of the
functions that \code{\link[=cacs_run]{cacs_run()}} runs: \code{\link[=cacs_isochrone]{cacs_isochrone()}},
\code{\link[=cacs_acs_prefetch]{cacs_acs_prefetch()}}, \code{\link[=cacs_intersect_weight]{cacs_intersect_weight()}}, \code{\link[=cacs_propagate_moe]{cacs_propagate_moe()}},
and \code{\link[=cacs_derive_rates]{cacs_derive_rates()}}. Their Value sections describe the attributes.
}
\details{
The output starts with a title and the kind of object, which is also kept
in \code{object_type} and decides the sections that follow:
\describe{
\item{\code{"cacs_run_result"}}{A result of \code{\link[=cacs_run]{cacs_run()}}. For a result with
\code{output = "both"}, its element \code{long} is described. The sections are
"Object", "Run", "Rates", and "Cache". "Run" shows the routing service
and profile, the drive times, the numbers of sites, and the running
time, as \code{\link[=print.cacs_run_result]{print.cacs_run_result()}} does, and "Rates" the lines of
"Rate Derivation" below. For the list-column form, "Rate rows" is 0,
because the rates are inside its \code{derived_rates} column. "Cache" shows
values of \code{\link[=cacs_get_cache_state]{cacs_get_cache_state()}} for the R session at the time of
the call.}
\item{\code{"weighted_seam"}, \code{"propagated_seam"}, \code{"derived_rates"}}{The
results of \code{\link[=cacs_intersect_weight]{cacs_intersect_weight()}}, \code{\link[=cacs_propagate_moe]{cacs_propagate_moe()}}, and
\code{\link[=cacs_derive_rates]{cacs_derive_rates()}}, told apart by their attributes; another tibble
is \code{"tbl_df"}. After "Object", "Carrier Table" summarizes the
\code{cacs_aggregation_carriers} attribute, the table of weighted totals and
means, with their variances, that \code{\link[=cacs_intersect_weight]{cacs_intersect_weight()}} keeps for
the later steps: the number of rows, the numbers of distinct variables
and sites, how many rows have a missing total, and the range of
\code{weight_sum}. \code{\link[=cacs_derive_rates]{cacs_derive_rates()}} removes that attribute, and for its
result the section reads \verb{Carrier attribute: absent or already consumed.} "MOE Propagation" shows values of
\code{cacs_moe_provenance}, the record of how the margins of error (MOE)
were computed. "Rate Derivation" shows the number of rate rows and
values of \code{cacs_rate_provenance}, and "Aggregation" values of
\code{cacs_aggregation_provenance}.}
\item{\code{"isochrone_sf"}, \code{"acs_sf"}, \code{"sf"}}{\code{sf} objects told apart by
their columns: drive-time areas, such as a result of
\code{\link[=cacs_isochrone]{cacs_isochrone()}}, American Community Survey (ACS) data, such as a
result of \code{\link[=cacs_acs_prefetch]{cacs_acs_prefetch()}}, and others. "Spatial Provenance"
shows values of \code{cacs_isochrone_provenance} and \code{cacs_res_param}, and
"ACS Provenance" values of \code{cacs_acs_provenance}.}
\item{\code{"unknown"}}{Any other object, with one section saying that there
is nothing to describe.}
}
A data frame that is not a tibble is described as a tibble only if it has
one of the package's attributes or one of the columns \code{site_id},
\code{drive_time_min}, \code{variable}, \code{estimand_family}, \code{ring_topology}, or
\code{failure_origin}. A value that is not recorded is shown as \code{n/a}. Two
sections differ: "Rates computed" lists the five rates even for an object
without rate rows, and "Carrier Table" says that the attribute is absent
or already consumed.

The description does not count the rate rows whose chosen formula for the
margin of error could not be used (\code{moe_fallback = TRUE}). The lines
about rates in "MOE Propagation", such as "C1 to C2 fallbacks", are
always 0 (see \code{\link[=cacs_propagate_moe]{cacs_propagate_moe()}}). "Formula downgrades" is the number
of rates that \code{formula_dispatch = "proportion_subset"} left on the ratio
formula. In "Aggregation", "Input sites" is the number of site and
drive-time pairs, and "Sites with data" is the number of rows with tracts
in the \code{\link[=cacs_intersect_weight]{cacs_intersect_weight()}} result, which has one row for each pair
and variable.

The description is printed as R messages, so \code{suppressMessages()} hides
it.
}
\examples{
# A bundled cacs_run() result for a 10-minute area in Birmingham, Alabama
out <- readRDS(system.file("extdata", "visual_walkthrough_fixture.rds",
                           package = "catchmentACS"))$run_result
desc <- cacs_describe(out)

# The package's attributes that out has
desc$attributes_present

}
\seealso{
Other result summaries:
\code{\link[=as_tibble.cacs_run_result]{as_tibble.cacs_run_result()}},
\code{\link[=cacs_summary_as_markdown]{cacs_summary_as_markdown()}},
\code{\link[=group_by.cacs_run_result]{group_by.cacs_run_result()}},
\code{\link[=print.cacs_run_result]{print.cacs_run_result()}},
\code{\link[=print.cacs_run_summary]{print.cacs_run_summary()}},
\code{\link[=summary.cacs_run_result]{summary.cacs_run_result()}}
}
\concept{result summaries}
