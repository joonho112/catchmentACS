% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/moe-helpers.R
\name{cacs_propagate_moe}
\alias{cacs_propagate_moe}
\title{Compute margins of error for drive-time area estimates}
\usage{
cacs_propagate_moe(data, formula = NULL, level = 0.9, verbose = TRUE, ...)
}
\arguments{
\item{data}{A tibble returned by \code{\link[=cacs_intersect_weight]{cacs_intersect_weight()}}, from which
rows may have been removed (for example with \code{[} or \code{dplyr::filter()})
but whose columns and attributes are kept, including
\code{cacs_aggregation_carriers} (a table of the weighted totals and means
with their variances, which this function reads). Rows for ACS codes
that \code{\link[=cacs_intersect_weight]{cacs_intersect_weight()}} does not combine (\code{estimand_family} is
\code{"metadata_only"}), such as codes of tables whose names begin with \code{C}
or \code{S}, make the function stop with an error; it runs once those rows
are removed. The row kept for a site and drive-time pair with no tracts
is returned unchanged.}

\item{formula}{A string, a list of strings named by values of \code{variable},
or \code{NULL} (the default), giving the formula for each row:
\code{"weighted_sum"}, \code{"weighted_mean"}, \code{"proportion_subset"}, or
\code{"general_ratio_conservative"} (see the "MOE formula families" section).
\code{NULL} gives each row the formula for its kind of estimate, and a string
is used for every row. In a list, the rows not named keep the formula
they would get with \code{NULL}. Each row accepts only the formula for its
kind of estimate; any other formula, a name not found in \code{variable}, or
an unknown formula gives an error.}

\item{level}{A single number between 0 and 1 (not a percentage) giving
the confidence level of the margins of error. The default is \code{0.9}.
Any other value, such as \code{0}, \code{1}, \code{90}, \code{NA}, \code{"0.9"}, or
\code{c(0.9, 0.95)}, gives an error.}

\item{verbose}{A logical value. With \code{TRUE} (the default), the function
shows a one-line summary when the step finishes (see the "Progress
messages" section of \code{\link[=cacs_run]{cacs_run()}}).}

\item{...}{Not used. The name \code{fallback_chain_max} is accepted with a
warning and has no effect; any other argument gives an error.}
}
\value{
The tibble \code{data} with the same rows in the same order, in which
\code{moe} (the margin of error) is recomputed at \code{level}. The columns that
record how it was computed are rewritten: \code{moe_formula_requested} and
\code{moe_formula_effective} (the formula chosen and the one used), and
\code{moe_fallback} and \code{moe_fallback_reason} (whether the chosen formula
could not be used, and why). Count, median, and per-person rows keep
\code{moe_fallback = FALSE} even when their margin of error is \code{NA}. Four
columns are added from the \code{cacs_aggregation_carriers} attribute:
\code{est_total} and \code{var_total_raw}, the weighted sum of the tract estimates
and its variance (used for counts), and \code{est_mean} and \code{var_mean_raw},
the weighted average and its variance (used for medians and per-person
values). With \code{options(cacs.return_se = TRUE)}, an \code{se} column is added
too: the standard error, \code{moe} divided by 1.645 at the 90 percent level
or by the normal quantile for another \code{level}.

The attributes of \code{data} are kept, and two are added.
\code{cacs_confidence_level} is the value of \code{level}, which
\code{\link[=cacs_derive_rates]{cacs_derive_rates()}} uses for the rates. \code{cacs_moe_provenance} is a
list recording the confidence level, the \code{formula} argument, and the
numbers of rows computed with Families A and B (see the "MOE formula
families" section). Its counts for rates are always 0, because the
rates are added later by \code{\link[=cacs_derive_rates]{cacs_derive_rates()}}, which counts them in its
\code{cacs_rate_provenance} attribute.
}
\description{
Computes a margin of error (MOE) for each estimate in the output of
\code{\link[=cacs_intersect_weight]{cacs_intersect_weight()}}, at the confidence level given by \code{level}. A
margin of error is the half-width of a confidence interval. The formula
depends on the kind of estimate and is recorded in the
\code{moe_formula_effective} column. American Community Survey (ACS) margins
of error are published at the 90 percent level; at the default
\code{level = 0.9}, the margins of error of counts, medians, and per-person
values are the same as those that \code{\link[=cacs_intersect_weight]{cacs_intersect_weight()}} returns.
}
\details{
The margins of error are combined as if the census tract estimates were
independent. The Census Bureau's handbook for ACS data users notes that
its approximation formulas leave out the covariance between estimates,
so a margin of error can be too small or too large depending on the
correlation between them (U.S. Census Bureau 2020, chapter 8). The
margin of error of a count, a median, or a per-person value is too small
if the tract estimates are positively correlated. For a rate, errors
that move the numerator and the denominator in the same direction partly
offset each other in the ratio, and the package does not compute the net
effect for a given rate. The area-based weights of the tracts are
treated as fixed, so the margins of error leave out two more sources of
error. One is the assumption that whatever a variable counts is spread
evenly over each tract's area. The other, for medians and per-person
values, is the weighting of tracts by area.
}
\section{MOE formula families}{

By default, each row gets the formula for the kind of estimate in its
\code{estimand_family} column. The four formulas are called Families A, B, C1,
and C2; the letters also appear in warnings and in the row counts of the
\code{cacs_moe_provenance} attribute. The formulas give the margin of error at
the 90 percent level, the level of the tract margins of error \eqn{M_i};
at another \code{level}, the result is multiplied by \eqn{z / 1.645}, where
\eqn{z} is the normal quantile for the level,
\code{qnorm(1 - (1 - level) / 2)}.
\itemize{
\item Family A, recorded as \code{"weighted_sum"}, is used for counts
(\code{"spatial_total"}). The margin of error is
\eqn{\sqrt{\sum_i (w_i M_i)^2}}{sqrt(sum((w_i * M_i)^2))}, where
\eqn{w_i} is the coverage weight of tract \eqn{i}, the share of its area
inside the drive-time area. This is the handbook's formula for a sum,
applied to the weighted tract estimates.
\item Family B, recorded as \code{"weighted_mean"}, is used for medians and
per-person values (\code{"median_proxy"} and \code{"area_weighted_scalar_proxy"}).
It is the same formula, with \eqn{w_i} proportional to the area that
tract \eqn{i} shares with the drive-time area and summing to one. This is
an approximation made by the package rather than one given in the
handbook.
\item Family C1, recorded as \code{"proportion_subset"}, is the handbook's formula
for a proportion, a ratio whose numerator is part of its denominator. For
the rate \eqn{\hat p = \hat X_{\mathrm{num}} /
  \hat X_{\mathrm{den}}}{p = X_num / X_den} of two weighted counts with
Family A margins of error \eqn{M_{\mathrm{num}}}{M_num} and
\eqn{M_{\mathrm{den}}}{M_den}, the margin of error is
\eqn{\sqrt{M_{\mathrm{num}}^2 - \hat p^2 M_{\mathrm{den}}^2} /
  \hat X_{\mathrm{den}}}{sqrt(M_num^2 - p^2 * M_den^2) / X_den}.
\item Family C2, recorded as \code{"general_ratio_conservative"}, is the handbook's
formula for a ratio whose numerator is not part of its denominator:
\eqn{\sqrt{M_{\mathrm{num}}^2 + \hat p^2 M_{\mathrm{den}}^2} /
  \left| \hat X_{\mathrm{den}} \right|}{sqrt(M_num^2 + p^2 * M_den^2) /
  abs(X_den)}. The package divides by the absolute value of the
denominator; for the five rates, whose denominators are counts, that is
the denominator itself. Like the other formulas it leaves out the
correlation between tract estimates (see Details).
}

C1 and C2 are used for the rows of the five rates in
\code{\link{cacs_acs_default_rates}} (\code{"derived_rate"}). By default every rate uses
C2, the ratio formula. The numerator of each of the five rates is part of
its denominator, so the rates are proportions, for which the handbook
gives C1, the proportion formula. The two formulas differ by
\eqn{2 \hat p^2 M_{\mathrm{den}}^2}{2 * p^2 * M_den^2} under the square
root, so C2 never gives the narrower margin; how much wider it is depends
on \eqn{\hat p M_{\mathrm{den}}}{p * M_den} relative to
\eqn{M_{\mathrm{num}}}{M_num} and varies from rate to rate.

If the value under the square root of C1 is negative, the row uses C2
instead, as the handbook advises (U.S. Census Bureau 2020, chapter 8).
The substitution is recorded in the row: \code{"general_ratio_conservative"}
in \code{moe_formula_effective}, \code{TRUE} in \code{moe_fallback}, and
\code{"negative_variance"} in \code{moe_fallback_reason}. How rates that cannot be
computed are marked is described in the "Rates that are NA" section of
\code{\link[=cacs_derive_rates]{cacs_derive_rates()}}.

The output of \code{\link[=cacs_intersect_weight]{cacs_intersect_weight()}} has no rate rows, so this function
applies only Families A and B; \code{\link[=cacs_derive_rates]{cacs_derive_rates()}} applies C1 and C2 to
the rates, as chosen by its \code{formula_dispatch} argument, at the level set
here.
}

\examples{
# Turn the cache off while this example runs (see ?cacs_set_cache).
old <- options(catchmentACS.cache_enabled = FALSE)

# Example data bundled with the package: the drive-time areas are circles
# with a radius of 1 km per minute, and the ACS data are made up.
library(sf)
iso <- readRDS(system.file("extdata", "legacy_2025_isochrones.rds",
                           package = "catchmentACS"))
acs <- readRDS(system.file("extdata", "sample_alabama_subset.rds",
                           package = "catchmentACS"))
iso_07 <- iso[iso$site_id == "AL_SITE_07" & iso$drive_time_min == 10, ]
agg <- cacs_intersect_weight(iso_sf = iso_07, acs_sf = acs,
                             verbose = FALSE)

# Margins of error at the default 90 percent level and at 95 percent
prop <- cacs_propagate_moe(agg, verbose = FALSE)
prop_95 <- cacs_propagate_moe(agg, level = 0.95, verbose = FALSE)
tibble::tibble(variable = prop$variable, estimate = prop$estimate,
               moe_90 = prop$moe, moe_95 = prop_95$moe,
               formula = prop$moe_formula_effective)

options(old)
}
\references{
U.S. Census Bureau (2020). \emph{Understanding and Using American
Community Survey Data: What All Data Users Need to Know}. Chapter 8,
Calculating Measures of Error for Derived Estimates.
}
\seealso{
\code{\link[=cacs_se_to_moe]{cacs_se_to_moe()}} and \code{\link[=cacs_moe_to_se]{cacs_moe_to_se()}} convert between
margins of error and standard errors. The mathematics behind these
formulas is in
\code{vignette("theory-moe-propagation", package = "catchmentACS")}
(\url{https://joonho112.github.io/catchmentACS/articles/theory-moe-propagation.html}).

Other steps of the calculation:
\code{\link[=cacs_acs_prefetch]{cacs_acs_prefetch()}},
\code{\link[=cacs_derive_rates]{cacs_derive_rates()}},
\code{\link[=cacs_intersect_weight]{cacs_intersect_weight()}},
\code{\link[=cacs_isochrone]{cacs_isochrone()}},
\code{\link[=cacs_run]{cacs_run()}}
}
\concept{steps of the calculation}
