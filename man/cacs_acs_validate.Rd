% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/checks.R
\name{cacs_acs_validate}
\alias{cacs_acs_validate}
\title{Check the form of ACS data for census tracts}
\usage{
cacs_acs_validate(x)
}
\arguments{
\item{x}{An \code{sf} object of ACS estimates with one row for each tract and
variable, or any other object to check.}
}
\value{
\code{TRUE}, invisibly, when every check passes. Otherwise the function
stops with an error of class \code{catchmentACS_error_schema} at the first
check that fails.
}
\description{
Checks that American Community Survey (ACS) estimates for census tracts,
such as a table from \code{tidycensus::get_acs()} with \code{geometry = TRUE}, have
the form that \code{\link[=cacs_intersect_weight]{cacs_intersect_weight()}} and the \code{acs} argument of
\code{\link[=cacs_run]{cacs_run()}} require. \code{\link[=cacs_run]{cacs_run()}} checks at the start only that \code{acs} is
an \code{sf} object, and makes the other checks at its intersection step,
after the routing step.
}
\details{
The checks are made in this order, and the first one that fails stops the
function with an error:
\enumerate{
\item \code{x} is an \code{sf} object.
\item Its coordinate reference system is NAD83 (EPSG:4269).
\item It has the columns \code{GEOID}, \code{NAME}, \code{variable}, \code{estimate}, \code{moe} (the
margin of error), and \code{geometry}.
\item It has at least one row.
\item Every \code{GEOID} has 11 digits, like a census tract code.
\item \code{estimate} and \code{moe} are numeric.
\item No tract has two rows for the same variable.
}

Whether \code{variable} holds ACS codes, and the values of \code{estimate} and
\code{moe}, are not checked. A name in place of an ACS code, such as \code{"pop"}
from \code{tidycensus::get_acs(variables = c(pop = "B01003_001"))}, gets \code{NA}
values, and \code{\link[=cacs_run]{cacs_run()}} then stops with an error about ACS codes that are
not combined. Census Bureau annotation codes, such as \code{-555555555}, pass
the checks. \code{\link[=cacs_intersect_weight]{cacs_intersect_weight()}} treats them as missing, and its
warning counts them, except a margin-of-error code next to a missing
estimate; the example data below have only such codes and give no
warning. A tract with no row for one of the variables is not reported,
and it is left out of that variable's total or average without a warning.
Whether every tract has one row for each variable can be checked with
\code{all(table(acs$GEOID, acs$variable) == 1)}.
}
\examples{
acs <- readRDS(system.file("extdata", "sample_alabama_subset.rds",
                           package = "catchmentACS"))
cacs_acs_validate(acs)

# tidycensus downloads the data, which needs a Census API key.
\dontrun{
my_acs <- tidycensus::get_acs(
  geography = "tract", variables = "B01003_001",
  state = "AL", year = 2023, geometry = TRUE, output = "tidy"
)
cacs_acs_validate(my_acs)
}
}
\seealso{
\code{\link[=cacs_acs_prefetch]{cacs_acs_prefetch()}} downloads ACS data in this form.

Other validation and conditions:
\code{\link[=cacs_capture_conditions]{cacs_capture_conditions()}},
\code{\link[=cacs_validate_iso]{cacs_validate_iso()}},
\code{\link[=cacs_validate_osrm_endpoint]{cacs_validate_osrm_endpoint()}},
\code{\link{catchmentACS-conditions}}
}
\concept{validation and conditions}
