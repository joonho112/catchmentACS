% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/checks.R
\name{cacs_acs_validate}
\alias{cacs_acs_validate}
\title{Validate a user-supplied ACS tract table}
\usage{
cacs_acs_validate(x)
}
\arguments{
\item{x}{An ACS tract \code{sf} tibble.}
}
\value{
Invisibly \code{TRUE} on success; aborts with a
\code{catchmentACS_error_schema} condition on the first failed check.
}
\description{
Checks that a user-supplied American Community Survey (ACS) \code{sf} tibble
(a spatial data frame of census tracts) meets the schema catchmentACS
expects before you feed it to \code{\link[=cacs_run]{cacs_run()}}. The six checks are: the object
is an \code{sf} object; its coordinate reference system is \code{NAD83}
(\code{EPSG:4269}); the required columns are present; every \code{GEOID} is an
11-digit tract identifier; \code{estimate} and \code{moe} (margin of error) are
numeric; and the table has at least one row. Run this before calling
\code{cacs_run(acs = my_sf, ...)} with your own pre-fetched ACS data.
}
\examples{
acs <- readRDS(system.file("extdata", "sample_alabama_subset.rds",
                           package = "catchmentACS"))
cacs_acs_validate(acs)

\dontrun{
my_acs <- tidycensus::get_acs(
  geography = "tract", variables = "B01003_001",
  state = "AL", year = 2023, geometry = TRUE, output = "tidy"
)
cacs_acs_validate(my_acs)
}
}
\seealso{
\code{\link[=cacs_acs_prefetch]{cacs_acs_prefetch()}} for fetching a schema-valid ACS table,
\code{\link[=cacs_intersect_weight]{cacs_intersect_weight()}} and \code{\link[=cacs_run]{cacs_run()}} for the consumers that
require this schema, and \code{\link[=cacs_validate_iso]{cacs_validate_iso()}} for the isochrone-side
counterpart.

Other validation and conditions:
\code{\link[=cacs_capture_conditions]{cacs_capture_conditions()}},
\code{\link[=cacs_validate_iso]{cacs_validate_iso()}},
\code{\link[=cacs_validate_osrm_endpoint]{cacs_validate_osrm_endpoint()}},
\code{\link{catchmentACS-conditions}}
}
\concept{validation and conditions}
