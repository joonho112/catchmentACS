% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-docs.R
\docType{data}
\name{cacs_acs_default_vars}
\alias{cacs_acs_default_vars}
\title{ACS variable codes used by default}
\format{
A named character vector of length 14. Each value is an ACS
variable code: \code{B} and five digits for the table, an underscore, and
three digits, such as \code{B17001_002}.
}
\source{
The codes are from the ACS detailed tables of the U.S. Census
Bureau. The variables and their names were chosen for this package.
}
\usage{
data("cacs_acs_default_vars")
}
\description{
The 14 American Community Survey (ACS) variable codes that
\code{\link[=cacs_acs_prefetch]{cacs_acs_prefetch()}} and \code{\link[=cacs_run]{cacs_run()}} use when \code{variables} is \code{NULL}
(the default). Ten of them are the numerators and denominators of the
five rates in \code{\link{cacs_acs_default_rates}}. The other four are total
population (\code{B01003_001}), total households (\code{B11001_001}), median
household income (\code{B19013_001}), and per capita income (\code{B19301_001}).

The names are short labels for the codes, such as \code{pov_below} for
\code{B17001_002}. The package uses only the codes, so results name each
variable by its code.
}
\examples{
data("cacs_acs_default_vars", package = "catchmentACS")
cacs_acs_default_vars
names(cacs_acs_default_vars)
length(cacs_acs_default_vars)  # 14
}
\keyword{datasets}
