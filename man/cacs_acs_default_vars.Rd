% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-docs.R
\docType{data}
\name{cacs_acs_default_vars}
\alias{cacs_acs_default_vars}
\title{Default ACS source variables for \code{cacs_acs_prefetch()}}
\format{
Named \code{character} vector of length 14. Each value is an ACS
variable code of the form \code{B#####_###} (five table digits, three
cell digits).
}
\source{
Author-curated mapping of analysis aliases to U.S. Census
Bureau ACS detailed-table variable codes.
}
\usage{
data("cacs_acs_default_vars")
}
\description{
Named character vector of 14 American Community Survey (ACS) variable
codes. These are the variables \code{\link[=cacs_acs_prefetch]{cacs_acs_prefetch()}} fetches by
default when \code{variables = NULL}. The names are short human-readable
aliases (e.g. \code{"pov_below"}, \code{"med_hh_inc"}) and the values are the
corresponding Census Bureau ACS table cell codes (e.g. \code{"B17001_002"}).

Together these source estimates supply the numerators, denominators,
and direct estimates that the package combines into its derived rate
indicators (see \code{\link[=cacs_derive_rates]{cacs_derive_rates()}}). For example, \code{ssi_hh}
(\code{"B19056_002"}) and \code{ssi_total} (\code{"B19056_001"}) form the
Supplemental Security Income rate.
}
\examples{
data("cacs_acs_default_vars")
cacs_acs_default_vars
names(cacs_acs_default_vars)
length(cacs_acs_default_vars)  # 14
}
\keyword{datasets}
