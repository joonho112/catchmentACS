% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run.R
\name{cacs_run}
\alias{cacs_run}
\title{Compute ACS estimates for drive-time areas around sites}
\usage{
cacs_run(
  sites,
  state,
  year = 2023,
  drive_times = c(5, 10, 15),
  variables = NULL,
  provider = c("osrm", "ors", "mapbox", "r5r"),
  weight_method = c("area", "population"),
  moe_formula = NULL,
  output = c("long", "list_column", "both"),
  precomputed_isochrones = NULL,
  cache_dir = NULL,
  acs = NULL,
  bg_pop_sf = NULL,
  rates = cacs_acs_default_rates,
  formula_dispatch = "general_ratio_conservative",
  iso_args = list(),
  acs_args = list(),
  weight_args = list(),
  moe_args = list(),
  rate_args = list(),
  verbose = TRUE
)
}
\arguments{
\item{sites}{A data frame with the columns \code{site_id}, \code{lon}, and \code{lat},
or an \code{sf} object of points with a \code{site_id} column, such as
\code{\link{cacs_alabama_sites}}. The coordinates are longitude and latitude on
WGS 84 (an \code{sf} object must be in EPSG:4326), and the \code{site_id} values
must be unique.}

\item{state}{A string giving one state, the District of Columbia, or
Puerto Rico, as a two-letter USPS abbreviation (such as \code{"AL"}) or a
two-digit FIPS code (such as \code{"01"}). Lowercase codes, such as \code{"al"},
give an error unless the ACS data are supplied through \code{acs}. Only the
tracts of this state are downloaded, so the part of a drive-time area in
another state adds nothing to the estimates unless \code{acs} also holds the
tracts of that state (see the Details of \code{\link[=cacs_intersect_weight]{cacs_intersect_weight()}},
which also say what happens to \code{acs_year}). Data for Alaska, Hawaii, or
Puerto Rico give an error at the intersection step.}

\item{year}{A single whole number giving the last year of the ACS 5-year
estimates, from 2009 to 2024; the default is 2023 (the 2019-2023
estimates).}

\item{drive_times}{A numeric vector of drive times in minutes; the default
is \code{c(5, 10, 15)}. When the areas are built, up to six whole numbers
from 1 to 60 are accepted.}

\item{variables}{A character vector of ACS variable codes, or \code{NULL} (the
default) for \code{\link{cacs_acs_default_vars}}; see \code{\link[=cacs_acs_prefetch]{cacs_acs_prefetch()}} for
the accepted codes. Codes outside \code{\link{cacs_acs_default_vars}} are
combined as counts, except those of tables \code{B19013} and \code{B25077}
(medians) and \code{B19301} (per capita income), so a median or per-person
value from any other table is added up over the tracts like a count,
without a warning (see \code{\link[=cacs_intersect_weight]{cacs_intersect_weight()}}).
Rates whose codes in \code{\link{cacs_acs_default_rates}} are left out are \code{NA},
with a warning.}

\item{provider}{A string giving the routing service used to build the
drive-time areas. \code{"osrm"} (the default) uses the Open Source Routing
Machine (OSRM) through a public server that needs no key; it requires
the osrm package. \code{"ors"} uses openrouteservice, which requires the
openrouteservice package and an API key in the \code{ORS_API_KEY}
environment variable or in \code{iso_args = list(ors_api_key = ...)}.
\code{"mapbox"} and \code{"r5r"} are accepted names, but building areas with them
is not implemented yet. Using them gives an error at the routing step,
so the ACS data are downloaded first unless \code{acs} is supplied. When
\code{precomputed_isochrones} is supplied, no areas are built, and \code{provider}
is only recorded in the \code{cacs_run_provenance} attribute and shown when
the result is printed.}

\item{weight_method}{A string giving the weighting method: \code{"area"} (the
default) or \code{"population"}. With \code{"area"}, counts are added up with each
tract weighted by the share of its area inside the drive-time area,
which assumes that whatever a variable counts is spread evenly over each
tract's area. The rates are ratios of these counts. Medians and
per-person values are averaged with weights proportional to each tract's
area inside the drive-time area. These weights ignore how many people
live in each tract, so the average can be far from the value for the
drive-time area when the tracts differ in population density (see
\code{\link[=cacs_intersect_weight]{cacs_intersect_weight()}}). \code{"population"} is not implemented yet and
gives an error before any step runs.}

\item{moe_formula}{A string, or a list of strings named by ACS variable
code, passed to \code{\link[=cacs_propagate_moe]{cacs_propagate_moe()}} as its \code{formula} argument, or
\code{NULL} (the default). Each ACS variable accepts only the formula for its
kind (\code{"weighted_sum"} for a count, \code{"weighted_mean"} for a median or
per-person value), and any other choice gives an error. The formulas for
the rates are chosen by \code{formula_dispatch}.}

\item{output}{A string choosing the form of the result, as described in
the Value section: \code{"long"} (the default), \code{"list_column"}, or \code{"both"}.}

\item{precomputed_isochrones}{An \code{sf} object of drive-time areas as
returned by \code{\link[=cacs_isochrone]{cacs_isochrone()}} (see \code{\link[=cacs_validate_iso]{cacs_validate_iso()}} for data from
other sources), or \code{NULL} (the default) to build them with \code{provider}.
When supplied, the routing step is skipped and every area in it is used.
\code{sites} and \code{drive_times} do not select areas from it; they are
recorded in the \code{cacs_run_provenance} attribute and shown when the
result is printed. A site in \code{sites} that has no area in it gets no rows
and no warning. If \code{precomputed_isochrones} has areas for sites that are
not in \code{sites}, the numbers of sites shown when the result is printed
are wrong (see \code{\link[=print.cacs_run_result]{print.cacs_run_result()}}). Areas with
\code{isochrone_empty = TRUE} or a \code{failure_reason} are treated as routing
failures (see Details).}

\item{cache_dir}{A path to the cache folder, or \code{NULL} (the default) to use
\code{\link[=cacs_cache_dir]{cacs_cache_dir()}}. It is used for the ACS data, drive-time areas, and
intersection results that \code{\link[=cacs_acs_prefetch]{cacs_acs_prefetch()}}, \code{\link[=cacs_isochrone]{cacs_isochrone()}}, and
\code{\link[=cacs_intersect_weight]{cacs_intersect_weight()}} save. A folder outside the temporary folder of
the R session is tidied as described in \code{\link[=cacs_cache_dir]{cacs_cache_dir()}}.}

\item{acs}{An \code{sf} object of ACS tract estimates as returned by
\code{\link[=cacs_acs_prefetch]{cacs_acs_prefetch()}} (see \code{\link[=cacs_acs_validate]{cacs_acs_validate()}} for data from other
sources), or \code{NULL} (the default) to download them with tidycensus;
downloading needs a Census API key (see \code{\link[=cacs_acs_prefetch]{cacs_acs_prefetch()}}). When
supplied, the download step is skipped, every variable in it is used
(\code{variables} is ignored), and \code{state} and \code{year} are only recorded in
the \code{cacs_run_provenance} attribute. Census Bureau annotation codes, such
as \code{-555555555}, are treated as missing, so a rate that uses such a value
is \code{NA} for the areas that include the tract, and two rows for the same
tract and variable give an error (see \code{\link[=cacs_intersect_weight]{cacs_intersect_weight()}}, which
also says when a warning is given). When a tract has a row for one code
of a rate and not for the other, for example after rows with a missing
estimate were removed, the rate is computed over different tracts
without a warning (see the "Tract counts for rates" section of
\code{\link[=cacs_derive_rates]{cacs_derive_rates()}}). Variable codes that
\code{\link[=cacs_intersect_weight]{cacs_intersect_weight()}} does not combine, such as \code{"C17002_002"} or
\code{"B19013A_001"}, make \code{\link[=cacs_propagate_moe]{cacs_propagate_moe()}} stop with an error.}

\item{bg_pop_sf}{An \code{sf} object of block-group population estimates, or
\code{NULL} (the default). It has no effect on the result (see
\code{weight_method}).}

\item{rates}{A named list giving the numerator and denominator ACS codes
of each rate. Only \code{\link{cacs_acs_default_rates}} (the default), the list
of the five built-in rates, is accepted; any other list, including a
subset or a reordering of it, gives an error.}

\item{formula_dispatch}{A string choosing the margin-of-error formula for
the five rates. \code{"general_ratio_conservative"} (the default) uses the
ratio formula for every rate. \code{"auto"} and \code{"proportion_subset"} use the
proportion formula for \code{poverty_rate} and \code{labor_force_participation}
and keep the ratio formula for \code{snap_rate}, \code{ssi_rate}, and
\code{unemp_rate}; \code{"proportion_subset"} also gives a warning naming those
three.}

\item{iso_args}{A named list of arguments passed on to \code{\link[=cacs_isochrone]{cacs_isochrone()}}
when the areas are built, or an empty list (the default). The accepted
names are \code{osrm_mode}, \code{ors_api_key}, \code{mapbox_token}, \code{r5r_core},
\code{profile}, \code{osm_snapshot_date}, and \code{res}; other names give a warning
and are dropped. Because \code{osrm.server} is not accepted, another OSRM
server is used by setting \code{osrm_mode = "docker"} and the option
\code{catchmentACS.osrm_docker_server} (see the "OSRM servers" section of
\code{\link[=cacs_isochrone]{cacs_isochrone()}}).}

\item{acs_args}{A named list of arguments passed on to
\code{\link[=cacs_acs_prefetch]{cacs_acs_prefetch()}} when \code{acs} is \code{NULL}, or an empty list (the
default). The accepted names are \code{survey}, \code{geography}, \code{force_refresh},
and \code{write_gpkg}; other names give a warning and are dropped.}

\item{weight_args}{A named list of arguments passed on to
\code{\link[=cacs_intersect_weight]{cacs_intersect_weight()}}, or an empty list (the default). The accepted
names are \code{min_weight} and \code{keep_tract_audit}; other names give a
warning and are dropped.}

\item{moe_args}{A named list of arguments passed on to
\code{\link[=cacs_propagate_moe]{cacs_propagate_moe()}}, or an empty list (the default). The accepted
names are \code{level}, which sets the confidence level of all the margins of
error (0.9 by default), and \code{fallback_chain_max}, which has no effect
and gives a warning (see \code{\link[=cacs_propagate_moe]{cacs_propagate_moe()}}). The name \code{formula}
gives an error
(\code{moe_formula} sets it), and other names give a warning and are dropped.}

\item{rate_args}{A named list of arguments for \code{\link[=cacs_derive_rates]{cacs_derive_rates()}}, or
an empty list (the default). No names are accepted: \code{formula_dispatch}
gives an error (the argument \code{formula_dispatch} sets it), and any other
name gives a warning and is dropped.}

\item{verbose}{A logical value, passed to each step that \code{cacs_run()}
runs. If \code{TRUE} (the default), messages report which steps run and how
each step progresses; see the "Progress messages" section.}
}
\value{
With \code{output = "long"} (the default), a tibble that also has the
class \code{cacs_run_result}, with one row for each site, drive time, and
variable, where the variable is an ACS variable or one of the five
rates. With \code{output = "list_column"}, a tibble of the same class with
one row for each site and drive time. With \code{output = "both"}, a plain
list with the elements \code{long} and \code{list_column}, one of each form.

The long form has these columns:
\describe{
\item{\code{site_id}, \code{drive_time_min}, \code{variable}}{The site, the drive
time in minutes, and the ACS variable code or rate name.}
\item{\code{ring_topology}}{Always \code{"cumulative"}: each drive-time area
contains the shorter ones.}
\item{\code{estimate}, \code{moe}}{The estimate and its margin of error, at the
confidence level given by the \code{cacs_confidence_level} attribute. For
an ACS variable, both are \code{NA} when a tract's estimate is missing,
and \code{moe} is \code{NA} when a tract's margin of error is missing.}
\item{\code{weight_sum}}{The sum of the coverage weights of the tracts
combined (a tract's coverage weight is the share of its area inside
the drive-time area); for a rate, the smaller of the sums for its
numerator and denominator, and \code{NA} on a rate row whose
\code{failure_origin} is \code{"carrier"}.}
\item{\code{n_tracts}}{The number of tracts combined; \code{NA} on rate rows.}
\item{\code{n_tracts_num}, \code{n_tracts_den}}{On rate rows, the numbers of
tracts combined for the numerator and for the denominator; \code{NA} on
other rows and on a rate row whose \code{failure_origin} is \code{"carrier"}
(see \code{\link[=cacs_derive_rates]{cacs_derive_rates()}}).}
\item{\code{provider}, \code{profile}, \code{osm_snapshot_date}}{The routing service,
the routing profile (such as \code{"car"}), and the date of the
OpenStreetMap data, as recorded on the drive-time areas.}
\item{\code{acs_year}}{The ACS year from the \code{cacs_provenance} attribute
that \code{\link[=cacs_acs_prefetch]{cacs_acs_prefetch()}} puts on the ACS data; \code{NA} when the data
have no such attribute, as with the sample ACS data in the package.}
\item{\code{weight_method}, \code{weight_basis}}{The weighting method (\code{"area"})
and the weights used (see the \code{weight_method} argument): \code{"coverage"}
for counts and rates, and \code{"area_mean"} for medians and per-person
values.}
\item{\code{estimand_family}}{The kind of quantity: \code{"spatial_total"} (a
count), \code{"median_proxy"} (a median), \code{"area_weighted_scalar_proxy"}
(a per-person value such as per capita income), or \code{"derived_rate"}
(a rate).}
\item{\code{moe_formula_requested}, \code{moe_formula_effective}}{The
margin-of-error formula chosen, through \code{moe_formula} or
\code{formula_dispatch} (see \code{\link[=cacs_derive_rates]{cacs_derive_rates()}} for the rates), and the
one used: \code{"weighted_sum"}, \code{"weighted_mean"}, \code{"proportion_subset"}
(the proportion formula), or \code{"general_ratio_conservative"} (the
ratio formula). A rate row whose \code{failure_origin} is \code{"carrier"} has
no margin of error, and both columns then name the formula that was
chosen for it rather than one that was used.}
\item{\code{moe_fallback}, \code{moe_fallback_reason}}{For a rate, whether the
chosen formula could not be used, and why: \code{"negative_variance"}
(the ratio formula was used instead) or \code{"zero_denominator"} (the
rate and its margin of error are \code{NA}); \code{"n/a"} otherwise.}
\item{\code{failure_origin}}{The step at which the row failed: \code{"none"};
\code{"isochrone"} (the routing service returned no area; the row's
estimates and most other values are \code{NA}); \code{"intersection"} (no
tract is left for the area; the pair has one such row, with
\code{variable = NA} and \code{n_tracts = 0}); or \code{"carrier"} (the rate is
\code{NA} because its numerator or denominator, or the margin of error of
either, is missing, as for the rates of a pair with no tract).}
\item{\code{weight_uncertainty_propagated}}{\code{FALSE}: the margins of error
treat the weights as fixed.}
}
The columns \code{est_total}, \code{var_total_raw}, \code{est_mean}, and
\code{var_mean_raw} hold the weighted sums and averages of the tract
estimates, with their variances, from which \code{estimate} and \code{moe} are
computed (see \code{\link[=cacs_propagate_moe]{cacs_propagate_moe()}}). They are \code{NA} on rate rows. With
\code{options(cacs.return_se = TRUE)}, a column \code{se} (the standard error)
follows; it is also \code{NA} on rate rows.

The list-column form has these columns:
\describe{
\item{\code{site_id}}{The site.}
\item{\code{lon}, \code{lat}}{The values of the columns of the same names in
\code{sites}, or \code{NA} when \code{sites} has no such columns, even if it is an
\code{sf} object such as \code{\link{cacs_alabama_sites}}; the point coordinates
are not used.}
\item{\code{drive_time_min}}{The drive time in minutes.}
\item{\code{isochrone}}{The drive-time area, as a one-row \code{sf} object.}
\item{\code{acs_estimates}, \code{derived_rates}}{Tibbles of the rows of the long
result for that site and drive time: the ACS variables and the
rates.}
\item{\code{metadata}}{A list of \code{provider}, \code{profile}, \code{osm_snapshot_date},
\code{acs_year}, \code{weight_method}, \code{failure_origin}, and \code{n_tracts}, taken
from the first row of \code{acs_estimates}.}
}

Both forms have these attributes:
\describe{
\item{\code{cacs_schema_version}}{The version label (\code{"1.0"}) of the
column layout.}
\item{\code{cacs_run_provenance}}{A list recording how the result was
produced. It gives the settings of the call: \code{state}, \code{year},
\code{drive_times}, \code{provider}, \code{weight_method}, \code{moe_formula},
\code{formula_dispatch}, \code{output_format}, \code{level} (the confidence
level), and \code{min_weight}. It records which steps were skipped
because their input was supplied, in \code{bypass_iso}, \code{bypass_acs},
and \code{execution_path}. It also holds counts of sites and of pairs
that succeeded or failed, the time of the run, and the package and
R versions.}
\item{\code{cacs_run_warnings}}{A list of the warnings given by each step,
in the elements \code{acs_prefetch}, \code{isochrone}, \code{intersect_weight},
\code{propagate_moe}, and \code{derive_rates}; when pairs failed at the
routing step, the element \code{orchestrator} has an entry whose
\code{failed_pairs} lists them, with the reason.}
\item{\code{cacs_aggregation_provenance}, \code{cacs_moe_provenance},
\code{cacs_rate_provenance}}{The settings and counts recorded by
\code{\link[=cacs_intersect_weight]{cacs_intersect_weight()}}, \code{\link[=cacs_propagate_moe]{cacs_propagate_moe()}}, and
\code{\link[=cacs_derive_rates]{cacs_derive_rates()}}; the last includes the formula chosen for each
rate.}
\item{\code{cacs_confidence_level}}{The confidence level of the margins of
error (0.9 unless \code{moe_args} sets another \code{level}).}
\item{\code{cacs_run_result_metadata}}{A list that \code{print()} and
\code{summary()} use: the time the result was created, the package
version, the routing service and profile, and the drive times. It
also holds counts of sites, the running time
(\code{wall_clock_seconds}), \code{skipped_geoids}, and counts of rate rows
by \code{moe_fallback_reason} (\code{moe_fallback_summary}).}
}
The long form also keeps \code{skipped_geoids}, the tracts skipped because
they have no area (see \code{\link[=cacs_intersect_weight]{cacs_intersect_weight()}}), and
\code{cacs_rate_audit}, the result of the check of each rate against a fixed
range that \code{options(catchmentACS.audit_rates = TRUE)} turns on (see
\code{\link[=cacs_derive_rates]{cacs_derive_rates()}}). With
\code{weight_args = list(keep_tract_audit = TRUE)}, it also keeps
\code{cacs_tract_audit}, a table of the tracts in each area with their
coverage weights. \code{\link[=cacs_describe]{cacs_describe()}} prints a summary drawn from these
attributes.
}
\description{
Builds drive-time areas around each site, one for each drive time, and
combines the American Community Survey (ACS) estimates of the census
tracts that overlap each area. These areas are also called isochrones.
Returns a table with an estimate and its margin of error (the half-width
of a confidence interval) for every site, drive time, and variable, plus
rows for five rates such as the poverty rate
(\code{\link{cacs_acs_default_rates}}). The function runs
\code{\link[=cacs_acs_prefetch]{cacs_acs_prefetch()}}, \code{\link[=cacs_isochrone]{cacs_isochrone()}}, \code{\link[=cacs_intersect_weight]{cacs_intersect_weight()}},
\code{\link[=cacs_propagate_moe]{cacs_propagate_moe()}}, and \code{\link[=cacs_derive_rates]{cacs_derive_rates()}} in that order; each of
them can also be called on its own. Supplying \code{precomputed_isochrones} or
\code{acs} skips the corresponding step, and with both supplied no routing
service or Census API key is needed.
}
\details{
Margins of error are at the 90 percent level unless \code{moe_args} sets
another \code{level}, and they treat the tract estimates as independent (see
\code{\link[=cacs_propagate_moe]{cacs_propagate_moe()}}). By default, every rate uses the formula that the
Census Bureau's handbook gives for a ratio of two estimates. The five
rates are proportions, for which the handbook gives a separate formula
whose margins are never wider; \code{formula_dispatch} can select it for
\code{poverty_rate} and \code{labor_force_participation}.

Site and drive-time pairs for which the routing service returns no area
stay in the result with \code{NA} values and \code{failure_origin = "isochrone"}
(\code{failure_origin} records the step at which a row failed), and a warning
reports how many pairs failed. A routing failure stops the function in two
cases: no pair succeeds, or the routing service refuses a request because
its request limit has been reached or an API key is not accepted.
}
\section{Progress messages}{

The five steps report their progress with messages, whether they are run
by \code{cacs_run()} or called on their own. Each step counts its work in
units: sites for \code{\link[=cacs_isochrone]{cacs_isochrone()}}, site and drive-time pairs for
\code{\link[=cacs_intersect_weight]{cacs_intersect_weight()}}, and a single unit for the other three steps. A
step can show a summary line when it finishes, with how many units
succeeded out of how many, the time taken, and any failures. It can also
show a progress line as each unit finishes. An example is
\code{Intersect+weight: 21/60 (ETA 0:01) - AL_SITE_07/15min}, where \code{ETA}
is the estimated time left. \code{\link[=cacs_propagate_moe]{cacs_propagate_moe()}} and
\code{\link[=cacs_derive_rates]{cacs_derive_rates()}} show at most the summary line.

The first of these rules that applies decides what a step shows:
\enumerate{
\item With \code{verbose = FALSE}, no progress messages.
\item If the environment variable \code{CACS_QUIET} is \code{"1"} (other values are
ignored), no progress messages.
\item If \code{getOption("catchmentACS.progress")} is \code{"off"}, no progress
messages; if it is \code{"force"}, the summary line and the progress lines,
whatever the number of units.
\item Otherwise, as with \code{"auto"} or when the option is not set (the
default), the summary line alone for fewer than five units, and the
summary line and the progress lines for five or more.
}

With more than 50 units, only the first progress line, every tenth line
after it, and the last line are shown;
\code{options(catchmentACS.progress_throttle = k)} changes ten to \code{k}. A step
whose result is read from the cache shows no progress messages. These
settings do not affect warnings.

With \code{verbose = TRUE}, \code{cacs_run()} also shows messages of its own. A
message at the start says which steps will run, and one message is shown
for each step skipped because \code{acs} or \code{precomputed_isochrones} was
supplied. At the end, a message gives the numbers of site and drive-time
pairs that succeeded and failed. In the same way,
\code{\link[=cacs_intersect_weight]{cacs_intersect_weight()}} shows a message before it starts, and
\code{\link[=cacs_acs_prefetch]{cacs_acs_prefetch()}} one before it downloads. Rules 2 and 3 leave these
messages on; \code{verbose = FALSE} turns them off. With
\code{output = "list_column"} or \code{"both"}, a message about the \code{isochrone}
column is shown on every call, whatever \code{verbose} is.

All of these are ordinary R messages, so \code{suppressMessages()} also hides
them. Progress lines have the condition class
\code{catchmentACS_message_progress_tick} and summary lines the class
\code{catchmentACS_message_progress_summary}. Both also have the class
\code{catchmentACS_message_progress}, as do the messages of \code{cacs_run()} and
\code{\link[=cacs_intersect_weight]{cacs_intersect_weight()}} that \code{verbose = FALSE} turns off, so
\code{\link[=cacs_capture_conditions]{cacs_capture_conditions()}} with that class in \code{classes} collects all of
them. The condition classes of the package are listed in
\code{\link{catchmentACS-conditions}}.
}

\examples{
# Turn the cache off while this example runs (see ?cacs_set_cache).
old <- options(catchmentACS.cache_enabled = FALSE)

# Example data bundled with the package: the drive-time areas are circles
# with a radius of 1 km per minute, and the ACS data are made up. The
# routing columns of the areas, such as provider, hold fixed values that
# do not come from a routing service, and the result repeats some of them.
library(sf)
iso <- readRDS(system.file("extdata", "legacy_2025_isochrones.rds",
                           package = "catchmentACS"))
acs <- readRDS(system.file("extdata", "sample_alabama_subset.rds",
                           package = "catchmentACS"))
iso_07 <- iso[iso$site_id == "AL_SITE_07" & iso$drive_time_min == 10, ]
# The site at the center of these areas
site_07 <- data.frame(site_id = "AL_SITE_07", lon = -85.365, lat = 31.655)

# With both the drive-time areas and the ACS data supplied, no routing
# service, Census API key, or internet connection is needed.
out <- cacs_run(site_07, state = "AL", drive_times = 10,
                precomputed_isochrones = iso_07, acs = acs,
                verbose = FALSE)

# The five rates for the 10-minute area of AL_SITE_07
tibble::as_tibble(out) |>
  dplyr::filter(variable \%in\% names(cacs_acs_default_rates)) |>
  dplyr::select(variable, estimate, moe)

options(old)

# Needs a Census API key, and builds the areas of five sites on the public
# OSRM demo server, which limits the requests it accepts.
\dontrun{
library(sf)
out_live <- cacs_run(cacs_alabama_sites[1:5, ], state = "AL", year = 2023,
                     drive_times = c(5, 10, 15), provider = "osrm")

# The poverty rate for each site and drive time, highest first
tibble::as_tibble(out_live) |>
  dplyr::filter(variable == "poverty_rate") |>
  dplyr::arrange(dplyr::desc(estimate)) |>
  dplyr::select(site_id, drive_time_min, estimate, moe)
}
}
\references{
U.S. Census Bureau (2020). \emph{Understanding and Using American
Community Survey Data: What All Data Users Need to Know}. Chapter 8,
Calculating Measures of Error for Derived Estimates.
}
\seealso{
The help pages \code{\link[=print.cacs_run_result]{print.cacs_run_result()}},
\code{\link[=summary.cacs_run_result]{summary.cacs_run_result()}}, and \code{\link[=as_tibble.cacs_run_result]{as_tibble.cacs_run_result()}} describe
how the result is printed, summarized, and converted to a plain tibble.
\code{vignette("getting-started", package = "catchmentACS")}
(\url{https://joonho112.github.io/catchmentACS/articles/getting-started.html})
walks through a first run, and
\code{vignette("methodology", package = "catchmentACS")}
(\url{https://joonho112.github.io/catchmentACS/articles/methodology.html})
describes the calculations.

Other steps of the calculation:
\code{\link[=cacs_acs_prefetch]{cacs_acs_prefetch()}},
\code{\link[=cacs_derive_rates]{cacs_derive_rates()}},
\code{\link[=cacs_intersect_weight]{cacs_intersect_weight()}},
\code{\link[=cacs_isochrone]{cacs_isochrone()}},
\code{\link[=cacs_propagate_moe]{cacs_propagate_moe()}}
}
\concept{steps of the calculation}
