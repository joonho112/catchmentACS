% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cache.R
\name{cacs_clear_cache}
\alias{cacs_clear_cache}
\title{Clear cached catchmentACS artifacts}
\usage{
cacs_clear_cache(
  namespace = c("all", "isochrone", "acs", "acs_test", "intersect"),
  confirm = interactive()
)
}
\arguments{
\item{namespace}{Which cache to clear: one of \code{"all"}, \code{"isochrone"},
\code{"acs"}, \code{"acs_test"}, or \code{"intersect"}.}

\item{confirm}{Logical; defaults to \code{interactive()}. When \code{TRUE}, prompts
before deletion. The prompt is skipped in non-interactive sessions and
when the environment variable \code{CACS_NO_CONFIRM = "1"} is set.}
}
\value{
Invisibly, a tibble with one row per cleared namespace and columns
\code{namespace}, \code{n_removed}, and \code{bytes_freed}.
}
\description{
Removes the \code{.rds} files, their \code{.fingerprint} sidecars, and any leftover
temporary files from the requested namespace subdirectories of
\code{\link[=cacs_cache_dir]{cacs_cache_dir()}}, then resets the in-session counters and memoised path
lookup so the next call starts from a clean slate.
}
\examples{
\dontrun{
cacs_clear_cache("isochrone", confirm = FALSE)
}
}
\seealso{
\code{\link[=cacs_cache_status]{cacs_cache_status()}}, \code{\link[=cacs_cache_dir]{cacs_cache_dir()}}, \code{\link[=cacs_set_cache]{cacs_set_cache()}},
\code{\link[=cacs_get_cache_state]{cacs_get_cache_state()}}; \code{\link[=cacs_acs_prefetch]{cacs_acs_prefetch()}} and \code{\link[=cacs_run]{cacs_run()}} write the
entries this removes.

Other cache and configuration:
\code{\link[=cacs_cache_dir]{cacs_cache_dir()}},
\code{\link[=cacs_cache_status]{cacs_cache_status()}},
\code{\link[=cacs_get_cache_state]{cacs_get_cache_state()}},
\code{\link[=cacs_set_cache]{cacs_set_cache()}}
}
\concept{cache and configuration}
