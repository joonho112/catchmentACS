% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cache.R
\name{cacs_clear_cache}
\alias{cacs_clear_cache}
\title{Delete saved results from the cache folder}
\usage{
cacs_clear_cache(
  namespace = c("all", "isochrone", "acs", "acs_test", "intersect"),
  confirm = interactive()
)
}
\arguments{
\item{namespace}{A string naming the kind of saved result to delete, which
is also the name of its subfolder:
\describe{
\item{\code{"all"} (the default)}{All four kinds.}
\item{\code{"isochrone"}}{Drive-time areas saved by \code{\link[=cacs_isochrone]{cacs_isochrone()}}.}
\item{\code{"acs"}}{American Community Survey (ACS) data saved by
\code{\link[=cacs_acs_prefetch]{cacs_acs_prefetch()}}.}
\item{\code{"acs_test"}}{ACS data saved in test mode (see \code{namespace_mode}
in \code{\link[=cacs_get_cache_state]{cacs_get_cache_state()}}).}
\item{\code{"intersect"}}{Results saved by \code{\link[=cacs_intersect_weight]{cacs_intersect_weight()}}.}
}}

\item{confirm}{A logical value. If \code{TRUE}, a question is asked first, and
files are deleted only if the answer is yes; in a non-interactive session
nobody can answer, so nothing is deleted. If \code{FALSE}, files are deleted
without a question. The default, \code{interactive()}, is \code{TRUE} only in an
interactive session, so a script run with \code{Rscript} deletes without
asking. The question is also skipped when the environment variable
\code{CACS_NO_CONFIRM} is \code{"1"}.}
}
\value{
A tibble, returned invisibly, with one row for each subfolder
cleared and the columns \code{namespace}, \code{n_removed} (the number of files
deleted, counting a saved result, its checksum file, and its GeoPackage
file as separate files), and
\code{bytes_freed} (their total size in bytes). It has no rows when the cache
folder does not exist or the answer to the question is no.
}
\description{
Deletes the saved results chosen by \code{namespace}, with their checksum files,
any temporary files left by an interrupted write, and the GeoPackage files
written by \code{cacs_acs_prefetch(write_gpkg = TRUE)}, from the cache folder
returned by \code{\link[=cacs_cache_dir]{cacs_cache_dir()}}. Other files in the folder and the
subfolders themselves are kept. A folder given in the \code{cache_dir} argument
of another function is not changed; to clear such a folder, first set
\code{options(catchmentACS.cache_dir = )} to it.
}
\details{
After deleting, \code{cacs_clear_cache()} sets the hit and miss counts of the
cleared subfolders to zero (see \code{\link[=cacs_get_cache_state]{cacs_get_cache_state()}}). If the cache
folder does not exist, a message says so and nothing is deleted.
}
\examples{
# Use a temporary cache folder, so that a cache folder you have set is
# left alone
old <- options(catchmentACS.cache_dir = file.path(tempdir(), "cacs-example"))
cacs_cache_status()
cacs_clear_cache("isochrone", confirm = FALSE)
options(old)
}
\seealso{
Other cache and configuration:
\code{\link[=cacs_cache_dir]{cacs_cache_dir()}},
\code{\link[=cacs_cache_status]{cacs_cache_status()}},
\code{\link[=cacs_get_cache_state]{cacs_get_cache_state()}},
\code{\link[=cacs_set_cache]{cacs_set_cache()}}
}
\concept{cache and configuration}
