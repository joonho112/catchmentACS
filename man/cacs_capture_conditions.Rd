% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/capture-conditions.R
\name{cacs_capture_conditions}
\alias{cacs_capture_conditions}
\title{Capture the package's messages and warnings in a table}
\usage{
cacs_capture_conditions(expr, classes = NULL, return_value = NULL)
}
\arguments{
\item{expr}{An expression to evaluate, such as a call to \code{\link[=cacs_run]{cacs_run()}}.}

\item{classes}{A character vector of the classes to capture, or \code{NULL}
(the default) for all messages and warnings of the package. Each
element can be a full class name, such as
\code{"catchmentACS_message_progress_summary"}, or \code{"message"} or
\code{"warning"} for all messages or all warnings of the package. It can
also be the part of a class name after
\code{catchmentACS_message_}, \code{catchmentACS_warning_}, or
\code{catchmentACS_error_}, such as \code{"water_tract_filter"} or \code{"progress"}.
A name that matches no class captures nothing, without an error.}

\item{return_value}{A string giving what to return: \code{"conditions"} for
the table of captured conditions, or \code{"both"} for a list with the value
of \code{expr} as well. \code{NULL} (the default) uses the option
\code{catchmentACS.capture_return_value}, which is \code{"conditions"} unless it
has been changed.}
}
\value{
With \code{return_value = "conditions"}, a tibble with one row for each
captured condition, in the order in which they were given, and these
columns:
\describe{
\item{\code{class}}{The most specific class of the condition, such as
\code{"catchmentACS_message_progress_summary"}.}
\item{\code{message}}{The text of the condition, as returned by
\code{conditionMessage()}.}
\item{\code{phase}}{The step that gave the condition: \code{"acs"} for
\code{\link[=cacs_acs_prefetch]{cacs_acs_prefetch()}}, \code{"isochrone"} for \code{\link[=cacs_isochrone]{cacs_isochrone()}},
\code{"intersect"} for \code{\link[=cacs_intersect_weight]{cacs_intersect_weight()}}, \code{"moe"} for
\code{\link[=cacs_propagate_moe]{cacs_propagate_moe()}}, \code{"rates"} for \code{\link[=cacs_derive_rates]{cacs_derive_rates()}}, \code{"run"}
for \code{\link[=cacs_run]{cacs_run()}} itself, and \code{"cache"} for \code{\link[=cacs_set_cache]{cacs_set_cache()}} and
\code{\link[=cacs_clear_cache]{cacs_clear_cache()}}. Messages and warnings about saved results have
the value of their step (some of those about ACS data saved in test
mode have \code{"acs_test"}; see \code{namespace_mode} in
\code{\link[=cacs_get_cache_state]{cacs_get_cache_state()}}). A progress line or summary line has the
label of its step, such as \code{"Intersect+weight"} (see the "Progress
messages" section of \code{\link[=cacs_run]{cacs_run()}}). The warning of
\code{\link[=cacs_validate_osrm_endpoint]{cacs_validate_osrm_endpoint()}} has \code{"isochrone"}, and the message and
warning of the maps about the nearest site have the name of the
function, such as \code{"cacs_plot_site_rates"}. The warnings of
\code{\link[=cacs_intersect_weight]{cacs_intersect_weight()}} about repaired geometries and the message
of \code{\link[=as_tibble.cacs_run_result]{as_tibble.cacs_run_result()}} have \code{NA}. The values are not the
names of the elements of the \code{cacs_run_warnings} attribute of a
\code{\link[=cacs_run]{cacs_run()}} result.}
\item{\code{timestamp}}{The time at which the condition was given, as a
date-time in UTC.}
\item{\code{call}}{The call in which the condition was given, as text, or
\code{NA} when there is none. For a condition given while \code{\link[=cacs_run]{cacs_run()}}
runs a step, the text can include the data passed to the step and be
very long.}
}
With \code{return_value = "both"}, a list with two elements: \code{result}, the
value of \code{expr}, and \code{conditions}, the tibble above.
}
\description{
Evaluates an expression and returns a tibble listing the standardized
catchmentACS messages and warnings given while it runs, that is, those
with the class \code{catchmentACS_condition} (see \code{\link{catchmentACS-conditions}}).
The captured messages and warnings are not shown.
}
\details{
The conditions are captured with \code{withCallingHandlers()}, so \code{expr} runs
to the end. Messages and warnings that are not captured, because they
come from other packages or do not match \code{classes}, are shown as usual.
Errors are not captured: an error in \code{expr} stops
\code{cacs_capture_conditions()}, and the conditions captured before it are
not returned.
}
\section{Keeping the value of the expression}{

With \code{return_value = "conditions"}, the value of \code{expr} is not returned.
An assignment made with \verb{<-} inside \code{expr} does not keep it either,
because \code{expr} is evaluated in a new environment:
\code{cacs_capture_conditions(acs <- cacs_acs_prefetch("AL"))} leaves \code{acs} as
it was. With \code{return_value = "both"}, the value is kept as the \code{result}
element of the list. An assignment made with \verb{<<-} inside \code{expr} also
keeps it, in the first variable of that name found by searching from the
environment in which \code{cacs_capture_conditions()} is called (see
\link[base:assignOps]{assignOps}).
}

\examples{
# Turn the cache off while this example runs (see ?cacs_set_cache).
old <- options(catchmentACS.cache_enabled = FALSE)

# Example data bundled with the package: the drive-time areas are circles
# with a radius of 1 km per minute, and the ACS data are made up.
library(sf)
iso <- readRDS(system.file("extdata", "legacy_2025_isochrones.rds",
                           package = "catchmentACS"))
acs <- readRDS(system.file("extdata", "sample_alabama_subset.rds",
                           package = "catchmentACS"))
iso_07 <- iso[iso$site_id == "AL_SITE_07" & iso$drive_time_min == 10, ]
site_07 <- data.frame(site_id = "AL_SITE_07", lon = -85.365, lat = 31.655)

# The messages of a run, captured instead of shown; out$result is the
# result of cacs_run()
out <- cacs_capture_conditions(
  cacs_run(site_07, state = "AL", drive_times = 10,
           precomputed_isochrones = iso_07, acs = acs),
  return_value = "both"
)
out$conditions[, c("class", "message")]

# Only the warnings: the range check on rates, turned on here, warns
# about two rates of the made-up data
old_audit <- options(catchmentACS.audit_rates = TRUE)
warned <- cacs_capture_conditions(
  cacs_run(site_07, state = "AL", drive_times = 10,
           precomputed_isochrones = iso_07, acs = acs, verbose = FALSE),
  classes = "warning"
)
warned[, c("class", "message")]
options(old_audit)

options(old)

# The download from the Census Bureau needs a Census API key.
\dontrun{
out <- cacs_capture_conditions(
  cacs_acs_prefetch(state = "AL", year = 2023),
  classes = "water_tract_filter",
  return_value = "both"
)
al <- out$result
out$conditions$message
}
}
\seealso{
Other validation and conditions:
\code{\link[=cacs_acs_validate]{cacs_acs_validate()}},
\code{\link[=cacs_validate_iso]{cacs_validate_iso()}},
\code{\link[=cacs_validate_osrm_endpoint]{cacs_validate_osrm_endpoint()}},
\code{\link{catchmentACS-conditions}}
}
\concept{validation and conditions}
