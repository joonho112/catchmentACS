% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run.R
\name{as_tibble.cacs_run_result}
\alias{as_tibble.cacs_run_result}
\title{Convert the result of \code{cacs_run()} to a plain tibble}
\usage{
\method{as_tibble}{cacs_run_result}(x, ..., rate_first = NULL)
}
\arguments{
\item{x}{A \code{cacs_run_result} object, as returned by \code{\link[=cacs_run]{cacs_run()}}.}

\item{...}{Passed to the data frame method of \code{\link[tibble:as_tibble]{tibble::as_tibble()}}, such
as \code{.name_repair}.}

\item{rate_first}{A logical value: \code{TRUE} puts the rate rows first and
\code{FALSE} keeps the order of the rows. With \code{NULL} (the default), the
option \code{catchmentACS.rate_first_default}, \code{FALSE} unless it has been
set, decides in the calls described above, and other calls keep the
order. A result without a \code{variable} column, such as the list-column
form, keeps its order either way.}
}
\value{
A tibble of class \code{c("tbl_df", "tbl", "data.frame")} with the
rows, columns, and attributes of \code{x}.
}
\description{
Removes the class \code{cacs_run_result} from a result of \code{\link[=cacs_run]{cacs_run()}}, so that
the table prints as an ordinary tibble, and keeps the other attributes.
The rows keep their order in \code{x} unless \code{rate_first} is \code{TRUE}; the rows
are then sorted by \code{site_id}, \code{drive_time_min}, and \code{variable}, with the
rows of the five rates before the other rows within each site and drive
time.
}
\details{
With \code{rate_first = NULL} (the default), the option
\code{catchmentACS.rate_first_default} decides, but only when \code{as_tibble()} is
called from code run in the global environment, such as the console, a
script, a document, or a function written in one of them. A conversion
made inside another package, such as dplyr, keeps the order, and so does a
call that passes the function itself, as in
\code{lapply(results, tibble::as_tibble)}. The first time the option puts the
rate rows first in an R session, a message says so.
}
\examples{
out <- readRDS(system.file("extdata", "visual_walkthrough_fixture.rds",
                           package = "catchmentACS"))$run_result

# By default the rows keep their order in out: the 14 ACS variables, then
# the five rates
tibble::as_tibble(out)$variable

# The rates first
tibble::as_tibble(out, rate_first = TRUE)$variable
}
\seealso{
Other result summaries:
\code{\link[=cacs_describe]{cacs_describe()}},
\code{\link[=cacs_summary_as_markdown]{cacs_summary_as_markdown()}},
\code{\link[=group_by.cacs_run_result]{group_by.cacs_run_result()}},
\code{\link[=print.cacs_run_result]{print.cacs_run_result()}},
\code{\link[=print.cacs_run_summary]{print.cacs_run_summary()}},
\code{\link[=summary.cacs_run_result]{summary.cacs_run_result()}}
}
\concept{result summaries}
