% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-docs.R
\docType{data}
\name{cacs_alabama_sites}
\alias{cacs_alabama_sites}
\title{Synthetic Alabama Pre-K sample sites}
\format{
An \code{sf} POINT object with 10 rows and 4 attribute columns
plus a geometry column. The coordinate reference system is
geographic WGS 84 (EPSG:4326).
\describe{
\item{\code{site_id}}{Character. Anonymous site identifier,
\code{"AL_SITE_01"} through \code{"AL_SITE_10"}.}
\item{\code{site_name}}{Character. Anonymous site label,
\code{"Sample Site 1"} through \code{"Sample Site 10"}.}
\item{\code{county_fips}}{Character. 5-digit county FIPS code of the
anchor city.}
\item{\code{region_label}}{Character. Coarse Alabama regional grouping
(e.g. \code{"Birmingham Metro"}, \code{"North"}).}
\item{\code{geometry}}{\code{sfc_POINT} geometry in EPSG:4326.}
}
}
\source{
Synthetic coordinates derived from public Alabama city
centroids (U.S. Census Bureau Gazetteer / GNIS) and perturbed with
a reproducible uniform jitter. The points are placeholders for
demonstration only and do not represent any real facility.
}
\usage{
data("cacs_alabama_sites")
}
\description{
A small, ready-to-use \code{sf} POINT layer of 10 anonymous Alabama
locations, supplied as example input for \code{\link[=cacs_run]{cacs_run()}} and the
worked examples throughout the documentation and vignettes. It lets
you exercise the full catchment pipeline without preparing your own
site file.

Coordinates are synthetic: they are derived from public Alabama city
centroids and then perturbed with a small reproducible random jitter
(about 0.007 degrees, roughly 0.5 mile, per axis). This preserves the
rough statewide geographic spread of Pre-K activity while ensuring no
point corresponds to a real facility. These are \strong{not} actual First
Class Pre-K facility locations and contain no administrative records.
}
\examples{
data("cacs_alabama_sites")
print(cacs_alabama_sites)
nrow(cacs_alabama_sites)               # 10
sf::st_crs(cacs_alabama_sites)$epsg    # 4326

# Run the catchmentACS pipeline (requires network + routing provider)
\dontrun{
result <- cacs_run(
  sites         = cacs_alabama_sites[1:3, ],
  state         = "AL",
  year          = 2023,
  drive_times   = c(5, 10, 15),
  variables     = "core",
  provider      = "osrm",
  weight_method = "area",
  output        = "long",
  verbose       = FALSE
)
head(result)
}
}
\keyword{datasets}
