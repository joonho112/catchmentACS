% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-docs.R
\docType{data}
\name{cacs_alabama_sites}
\alias{cacs_alabama_sites}
\title{Ten example sites near Alabama city centers}
\format{
An \code{sf} object (a data frame with a geometry column) with 10 rows
and 5 columns. The points are in longitude and latitude on WGS 84
(EPSG:4326).
\describe{
\item{\code{site_id}}{A string, \code{"AL_SITE_01"} to \code{"AL_SITE_10"}.}
\item{\code{site_name}}{A string, \code{"Sample Site 1"} to \code{"Sample Site 10"}.}
\item{\code{county_fips}}{A string giving the five-digit FIPS code of the
county that contains the point, such as \code{"01073"} (Jefferson County)
for the point near Birmingham.}
\item{\code{region_label}}{A string naming the part of the state, such as
\code{"Birmingham Metro"} or \code{"North"}; each site has a different one.}
\item{\code{geometry}}{The point.}
}
}
\source{
Made for this package from public coordinates of the centers of
ten Alabama cities: Birmingham, Mobile, Huntsville, Montgomery,
Tuscaloosa, Auburn, Decatur, Florence, Dothan, and Gadsden, in the order
of \code{site_id}. Each point was then moved by a random amount of up to
0.007 degrees of longitude and of latitude (less than 1 kilometer in
all), drawn with a fixed random seed.
}
\usage{
data("cacs_alabama_sites")
}
\description{
Ten made-up site locations in Alabama, as an \code{sf} object of points. Each
point lies near the center of an Alabama city. The points were not taken
from the locations of Pre-K classrooms or of any other facility, and the
data contain no administrative records.

The sites are the \code{sites} input of examples that need a routing service,
such as the one in \code{\link[=cacs_run]{cacs_run()}}, and the default \code{sites_df} of the map
functions, such as \code{\link[=cacs_plot_site_isochrone]{cacs_plot_site_isochrone()}}. The bundled files
\code{legacy_2025_sites.rds} and \code{legacy_2025_isochrones.rds}, used by several
examples that run offline, give the same \code{site_id} values to other
points, 130 to 440 kilometers away.
}
\examples{
library(sf)
data("cacs_alabama_sites", package = "catchmentACS")
print(cacs_alabama_sites)
nrow(cacs_alabama_sites)               # 10
sf::st_crs(cacs_alabama_sites)$epsg    # 4326

# Needs a Census API key, and routes three sites on the public OSRM demo
# server with the osrm package; that server limits the requests it accepts.
\dontrun{
library(sf)
result <- cacs_run(
  sites         = cacs_alabama_sites[1:3, ],
  state         = "AL",
  year          = 2023,
  drive_times   = c(5, 10, 15),
  variables     = "core",
  provider      = "osrm",
  weight_method = "area",
  output        = "long",
  verbose       = FALSE
)
head(result)
}
}
\keyword{datasets}
