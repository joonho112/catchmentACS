% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run.R
\name{cacs_summary_as_markdown}
\alias{cacs_summary_as_markdown}
\title{Format a summary table as a Markdown table}
\usage{
cacs_summary_as_markdown(summary_tbl, ...)
}
\arguments{
\item{summary_tbl}{A data frame, such as one of the elements
\code{rates_breakdown}, \code{rates_per_site}, and \code{rates_per_site_moe} of a
summary made by \code{\link[=summary.cacs_run_result]{summary.cacs_run_result()}}.}

\item{...}{Arguments passed to \code{\link[knitr:kable]{knitr::kable()}}, such as \code{digits},
\code{caption}, \code{col.names}, or \code{align}. The format is always \code{"pipe"}, so
\code{format} gives an error.}
}
\value{
A character vector of class \code{knitr_kable} with the lines of the
table, and of the caption if there is one. Printing it shows the table.
}
\description{
Formats a data frame as a Markdown pipe table for Quarto or R Markdown
documents and GitHub pages, using \code{\link[knitr:kable]{knitr::kable()}}. Numbers are rounded to
three decimals unless \code{digits} is given. The knitr package is required.
}
\details{
A table with a \code{site_id} column and a column for at least one of the five
rates, such as \code{rates_per_site} or \code{rates_per_site_moe}, is changed in two
ways. Its columns are put in the order \code{site_id}, \code{drive_time_min} (if
present), the rates in the order of \code{\link{cacs_acs_default_rates}}, and any
other columns. A caption is added unless \code{caption} is given. When the
cells are text, as in \code{rates_per_site_moe}, the caption says that they
show estimates with margins of error (half-widths of confidence
intervals), at the confidence level in the table's attribute
\code{cacs_confidence_level}, which \code{\link[=summary.cacs_run_result]{summary.cacs_run_result()}} sets. For a
table without that attribute, the caption gives no level.
}
\examples{
if (requireNamespace("knitr", quietly = TRUE)) {
  # A made-up table shaped like the rates_breakdown element of a summary
  summary_tbl <- tibble::tibble(
    variable = "poverty_rate",
    mean = 0.1234,
    sd = 0.0567,
    n_NA = 0L
  )
  cacs_summary_as_markdown(summary_tbl)
}
}
\seealso{
Other result summaries:
\code{\link[=as_tibble.cacs_run_result]{as_tibble.cacs_run_result()}},
\code{\link[=cacs_describe]{cacs_describe()}},
\code{\link[=group_by.cacs_run_result]{group_by.cacs_run_result()}},
\code{\link[=print.cacs_run_result]{print.cacs_run_result()}},
\code{\link[=print.cacs_run_summary]{print.cacs_run_summary()}},
\code{\link[=summary.cacs_run_result]{summary.cacs_run_result()}}
}
\concept{result summaries}
