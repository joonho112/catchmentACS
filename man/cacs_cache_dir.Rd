% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cache.R
\name{cacs_cache_dir}
\alias{cacs_cache_dir}
\title{Get the path of the cache folder}
\usage{
cacs_cache_dir(create = FALSE)
}
\arguments{
\item{create}{A logical value. If \code{FALSE} (the default), the path is
returned without creating the folder. If \code{TRUE}, the folder is created,
together with any missing parent folders, when it does not exist, and an
error is given if it cannot be created.}
}
\value{
A string giving the path of the cache folder.
}
\description{
Returns the path of the cache folder, where the package saves results on
disk for reuse. By default, \code{\link[=cacs_isochrone]{cacs_isochrone()}}, \code{\link[=cacs_acs_prefetch]{cacs_acs_prefetch()}}, and
\code{\link[=cacs_intersect_weight]{cacs_intersect_weight()}} each save their results in a subfolder of it
(\code{isochrone}, \code{acs}, and \code{intersect}), and a later call that matches an
earlier one reads the saved result instead of computing or downloading it
again. The help page of each function says what must match.
}
\details{
The cache folder is the first of these that is set and not empty:
\enumerate{
\item the option \code{catchmentACS.cache_dir};
\item the environment variable \code{CACS_CACHE_DIR};
\item a folder named \code{catchmentACS} inside the temporary folder of the R
session (see \code{\link[=tempdir]{tempdir()}}), which R deletes when the session ends.
}

The folder is worked out again at every call, so a change to the option or
the environment variable takes effect at once.
}
\section{How saved results are checked}{

Each saved result is an \code{.rds} file with a checksum file (\code{.fingerprint})
next to it. When a saved result is read, its checksum is computed again
and compared with the one in the checksum file. If the checksum file is
missing or not in the expected form, if the \code{.rds} file cannot be read, or
if the checksums differ, both files are deleted and the result is computed
or downloaded again. When saved American Community Survey (ACS) data are
read, \code{\link[=cacs_acs_prefetch]{cacs_acs_prefetch()}} also warns if they look incomplete.
}

\section{A cache folder that lasts between sessions}{

Results saved in the default folder are deleted when the R session ends.
To keep them for later sessions, set the option or the environment
variable to a folder that lasts, for example with this line in the R
startup file (see \link[base:Startup]{Startup}):

\if{html}{\out{<div class="sourceCode">}}\preformatted{options(catchmentACS.cache_dir = tools::R_user_dir("catchmentACS", "cache"))
}\if{html}{\out{</div>}}

\code{\link[tools:R_user_dir]{tools::R_user_dir()}} gives a folder for the cache files of one package,
inside the user's cache folder for R; where that is depends on the
operating system.

A cache folder outside the temporary folder of the session is tidied once
per session, the first time a result is read from it or saved in it while
the cache is on. These files are deleted:
\itemize{
\item results not used for 30 days, with their checksum and GeoPackage files.
Reading a saved result counts as using it. The option
\code{catchmentACS.cache_max_age_days} sets another number of days, and \code{Inf}
keeps results however old they are. Its value when the folder is first
used in the session is the one that counts, so set it in the R startup
file next to \code{catchmentACS.cache_dir}, for example
\code{options(catchmentACS.cache_max_age_days = 90)}; a value that is not a
single positive number counts as 30;
\item GeoPackage files as old as that without a result;
\item files left by an interrupted write, and results or checksum files
without their partner, when they are more than a day old.
}

Only files named the way the package names its saved files, in the
subfolders \code{isochrone}, \code{acs}, \code{acs_test}, and \code{intersect}, are deleted.

Versions 0.5.1 and earlier saved results in the user cache folder of the
operating system: \verb{~/Library/Caches/catchmentACS} on macOS,
\verb{~/.cache/catchmentACS} on Linux, and \code{catchmentACS/catchmentACS/Cache}
inside the folder named by the environment variable \code{LOCALAPPDATA} on
Windows.
The package no longer reads or deletes that folder; delete it yourself if
it is not needed.
}

\examples{
# The path of the cache folder, without creating the folder
cacs_cache_dir(create = FALSE)
}
\seealso{
Other cache and configuration:
\code{\link[=cacs_cache_status]{cacs_cache_status()}},
\code{\link[=cacs_clear_cache]{cacs_clear_cache()}},
\code{\link[=cacs_get_cache_state]{cacs_get_cache_state()}},
\code{\link[=cacs_set_cache]{cacs_set_cache()}}
}
\concept{cache and configuration}
