% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/intersect-weight.R
\name{cacs_intersect_weight}
\alias{cacs_intersect_weight}
\title{Aggregate ACS tract estimates to drive-time areas by area weighting}
\usage{
cacs_intersect_weight(
  iso_sf,
  acs_sf,
  bg_pop_sf = NULL,
  weight_method = c("area", "population"),
  min_weight = 1e-06,
  verbose = TRUE,
  keep_tract_audit = FALSE,
  cache_dir = NULL
)
}
\arguments{
\item{iso_sf}{An \code{sf} object of drive-time areas as returned by
\code{\link[=cacs_isochrone]{cacs_isochrone()}}, with one row for each site and drive time. Data from
other sources must have the same columns, which \code{\link[=cacs_validate_iso]{cacs_validate_iso()}}
checks, and each area must contain the shorter ones
(\code{ring_topology = "cumulative"}; see \code{\link[=cacs_rings_to_cumulative]{cacs_rings_to_cumulative()}}).}

\item{acs_sf}{An \code{sf} object of ACS estimates for census tracts as
returned by \code{\link[=cacs_acs_prefetch]{cacs_acs_prefetch()}}, with one row for each tract and
variable. Data from other sources must have the same columns, which
\code{\link[=cacs_acs_validate]{cacs_acs_validate()}} checks.}

\item{bg_pop_sf}{An \code{sf} object of block-group population estimates, or
\code{NULL} (the default). It has no effect on the result (see
\code{weight_method}).}

\item{weight_method}{A string giving the weighting method: \code{"area"} (the
default) or \code{"population"}, which is not implemented yet; using it gives
an error before any other work is done.}

\item{min_weight}{A single number in [0, 1); the default is \code{1e-6}.
Tracts whose coverage weight is at or below it are dropped (see step 4
of Details).}

\item{verbose}{A logical value. If \code{TRUE} (the default), progress messages
are shown: a summary line when the step finishes and, with five or more
site and drive-time pairs, also a line as each pair finishes. See the
"Progress messages" section of \code{\link[=cacs_run]{cacs_run()}} for how to turn them off or
show the lines for any number of pairs.}

\item{keep_tract_audit}{A logical value, \code{FALSE} (the default) or \code{TRUE}.
If \code{TRUE}, the result has a \code{cacs_tract_audit} attribute: a table with
one row for each tract used in each drive-time area. Its columns are
\code{site_id}, \code{drive_time_min}, \code{GEOID}, the tract's coverage weight
(\code{area_wt}), and the areas of the overlap (\code{int_area_m2}) and of the
tract (\code{tract_area_m2}) in square meters.}

\item{cache_dir}{A path to the cache folder, or \code{NULL} (the default) to use
\code{\link[=cacs_cache_dir]{cacs_cache_dir()}}. The result is saved in, and looked for in, its
\code{intersect} subfolder. A folder outside the temporary folder of the R
session is tidied as described in \code{\link[=cacs_cache_dir]{cacs_cache_dir()}}.}
}
\value{
A tibble with one row for each site, drive time, and ACS variable,
sorted by \code{site_id}, \code{drive_time_min}, and \code{variable}, followed by one
row for each site and drive-time pair with no tract left (see Details).
It has the columns that \code{\link[=cacs_run]{cacs_run()}} returns in its long form,
described in its Value section, except \code{est_total}, \code{var_total_raw},
\code{est_mean}, and \code{var_mean_raw}, which \code{\link[=cacs_propagate_moe]{cacs_propagate_moe()}} adds. The
main columns are:
\describe{
\item{\code{estimate}, \code{moe}}{The estimate and its margin of error, at the
90 percent level (see Details).}
\item{\code{weight_sum}, \code{n_tracts}}{The sum of the coverage weights of the
tracts combined, also on the rows for medians and per-person values,
and the number of those tracts, including any with a missing
estimate.}
\item{\code{estimand_family}, \code{weight_basis}}{The kind of quantity and the
weights used: \code{"spatial_total"} (a count) with \code{"coverage"};
\code{"median_proxy"} (a median) or \code{"area_weighted_scalar_proxy"} (a
per-person value) with \code{"area_mean"}; or \code{"metadata_only"} (a code
that is not combined) with \code{"none"}, and \code{\link[=cacs_propagate_moe]{cacs_propagate_moe()}}
gives an error for such rows.}
\item{\code{failure_origin}}{\code{"none"}, or \code{"intersection"} on the row of a
pair with no tract left.}
}
The result has these attributes:
\describe{
\item{\code{cacs_aggregation_carriers}}{A table of the weighted sums and
averages of the tract estimates, with their variances, for each
site, drive time, and variable. \code{\link[=cacs_propagate_moe]{cacs_propagate_moe()}} and
\code{\link[=cacs_derive_rates]{cacs_derive_rates()}} read it, and \code{\link[=cacs_derive_rates]{cacs_derive_rates()}} removes it.}
\item{\code{cacs_aggregation_provenance}}{A list recording \code{weight_method},
\code{min_weight}, \code{acs_year}, the time the result was computed, the
versions of catchmentACS, sf, GEOS, and PROJ, and counts of
variables and of missing estimates. \code{n_sites_input} is the number of
site and drive-time pairs, and \code{n_sites_with_data} and
\code{n_sites_empty} are the numbers of rows of the result with and
without tracts.}
\item{\code{skipped_geoids}}{The \code{GEOID} of each row of \code{acs_sf} skipped in
step 3 of Details, so a skipped tract appears once for each of its
variables; an empty character vector when nothing is skipped.}
}
It also has \code{cacs_schema_version}, the version label (\code{"1.0"}) of the
column layout, and, with \code{keep_tract_audit = TRUE}, \code{cacs_tract_audit}
(see that argument).
}
\description{
Combines the American Community Survey (ACS) estimates of the census
tracts that overlap each drive-time area (isochrone) into an estimate and
a margin of error for every site, drive time, and variable. Counts, such
as total population (\code{B01003_001}), are added up with each tract weighted
by the share of its area inside the drive-time area (its coverage weight),
which assumes that whatever a variable counts is spread evenly over each
tract's area. The assumption is made for each variable separately, and it
is a stronger one for a subgroup, such as the people below the poverty
level, than for the population as a whole. Medians and per-person values,
such as median household income (\code{B19013_001}) and per capita income
(\code{B19301_001}), are averaged over the overlapping tracts with weights
proportional to the area each tract shares with the drive-time area. This
average stands in for the median or per-person value of the drive-time
area, but its weights follow area and ignore how many people live in each
tract. It can therefore be far from that value when the overlapping tracts
differ in population density.
}
\details{
Like the ACS margins they are combined from, the margins of error are
half-widths of 90 percent confidence intervals, and they treat the
weights as fixed and the tract estimates as independent (see
\code{\link[=cacs_propagate_moe]{cacs_propagate_moe()}}). Rates, such as the poverty rate, are computed
later by \code{\link[=cacs_derive_rates]{cacs_derive_rates()}}, each as the ratio of two of these weighted
counts.

The function works in these steps.
\enumerate{
\item Checks the inputs. \code{iso_sf} must be in EPSG:4326 (longitude and
latitude on WGS 84) and \code{acs_sf} in EPSG:4269 (NAD83), as returned by
\code{\link[=cacs_isochrone]{cacs_isochrone()}} and \code{\link[=cacs_acs_prefetch]{cacs_acs_prefetch()}}, and two rows of \code{acs_sf}
for the same tract and variable give an error. Both must lie within a
box around the contiguous United States and the District of Columbia,
so data for Alaska, Hawaii, or Puerto Rico give an error. The codes
that the Census Bureau's data API puts in place of some estimates and
margins of error (-222222222, -333333333, -555555555, -666666666,
-888888888, and -999999999) are set to \code{NA}, with a warning that counts
them; a margin-of-error code next to a missing estimate is not counted.
Other negative values are used as they are. A warning is also given
when the bounding box of the drive-time areas extends beyond that of
the tracts (see below).
\item Transforms both inputs to EPSG:5070 (NAD83 / Conus Albers), an
equal-area projection, and measures all areas there, in square meters.
Invalid geometries are repaired, with a warning. For \code{iso_sf}, the
change from WGS 84 to NAD83 is the one that PROJ chooses. Without
datum grid files, PROJ treats the two as the same; with them, it can
move the areas by a meter or two. Results can therefore differ between
computers in the fourth or fifth significant digit.
\item Skips, with a warning, the tracts whose area is zero or not finite,
such as a water tract with an empty boundary, and stops with an error
if every tract is like that. The skipped tracts are listed in the
\code{skipped_geoids} attribute.
\item For each site and drive time, finds the tracts that overlap the area.
A tract's coverage weight is the area of its overlap divided by the
tract's area. Tracts with a coverage weight at or below \code{min_weight},
including tracts that only touch the edge of the area, are dropped.
Each area is used whole, so the 10-minute results include the tracts
of the 5-minute area.
\item Combines the remaining tracts for each variable. A count is the sum of
the tract estimates multiplied by their coverage weights, and a median
or per-person value is the average of the tract estimates weighted by
the area of each overlap. The margin of error is
\eqn{\sqrt{\sum_i (w_i M_i)^2}}{sqrt(sum((w_i * M_i)^2))}, where
\eqn{M_i} is the margin of error of tract \eqn{i} and \eqn{w_i} its
weight. For a count the weight is the coverage weight, and for a
median or per-person value it is the area share (see the "Coverage
weights and area shares" section). A missing estimate in any of the
tracts, including a code set to \code{NA} in step 1, makes the variable's
estimate and margin of error \code{NA}; a missing margin of error makes only
the margin of error \code{NA}. A rate that uses the variable is \code{NA} in both
cases (see \code{\link[=cacs_derive_rates]{cacs_derive_rates()}}). \code{\link[=cacs_acs_prefetch]{cacs_acs_prefetch()}} sets negative
margins of error to \code{NA}.
}

Whether a variable is a count, a median, or a per-person value is decided
by its ACS code. Tables \code{B19013} and \code{B25077} are medians, and table
\code{B19301} is a per-person value. Every other code of the form \code{B}, five
digits, an underscore, and three digits (such as \code{B17001_002}) is treated
as a count, so a median or per-person value from another table is added
up like a count. Other codes, such as those of tables whose names begin
with \code{C} or \code{S} or end with a letter (such as \code{B17001A}), are not combined
and get \code{NA} values.

Only the tracts in \code{acs_sf} are used: any part of a drive-time area outside
them (for example, across a state line) adds nothing, so counts come out
too small, and rates and medians come from the remaining tracts. The only
warning about this, in step 1, compares the bounding box of all the areas
in \code{iso_sf} with the bounding box of all the tracts in \code{acs_sf}: it is
given when the first extends more than 0.05 degrees (about 5 km) beyond
the second on some side. The tracts of a neighboring state can be
included in \code{acs_sf}, for example by combining two \code{\link[=cacs_acs_prefetch]{cacs_acs_prefetch()}}
results with \code{\link[dplyr:bind_rows]{dplyr::bind_rows()}}; the combined data no longer record the
ACS year, so \code{acs_year} in the result is \code{NA}.

A site and drive time can have no tract left after step 4: no tract
overlaps the area, the area is empty or appears twice in \code{iso_sf}, or
every tract is at or below \code{min_weight}. Such a pair gives one row with
\code{variable = NA}, \code{NA} values, \code{n_tracts = 0}, and
\code{failure_origin = "intersection"}. No warning is given. The same row is
given when the overlap calculation for a pair fails, so one failing pair
does not stop the others.

Results are cached (see \code{\link[=cacs_set_cache]{cacs_set_cache()}}) in the folder given by
\code{cache_dir} or \code{\link[=cacs_cache_dir]{cacs_cache_dir()}}: a call that matches an earlier one
returns the saved result without repeating steps 2 to 5 or their
warnings. The drive-time areas are matched by the \code{cache_key} that
\code{\link[=cacs_isochrone]{cacs_isochrone()}} attaches to its result together with their contents:
the geometries, the coordinate reference system, and the columns
\code{site_id}, \code{drive_time_min}, \code{provider}, \code{profile}, \code{osm_snapshot_date},
and \code{ring_topology}. A subset or an edited copy of a \code{\link[=cacs_isochrone]{cacs_isochrone()}}
result is therefore computed again, while the same areas in another row
order use the saved result. \code{options(catchmentACS.cache_intersect = FALSE)}
turns this cache off.
}
\section{Coverage weights and area shares}{

For one site, drive time, and variable, let \eqn{a_i} be the area of the
overlap between tract \eqn{i} and the drive-time area and \eqn{A_i} the
area of the tract. The tracts are those kept in step 4 of Details that
have a row for the variable in \code{acs_sf}; a tract without such a row is
left out, without a warning. A count then does not include that tract,
and \code{n_tracts} on the rows of that variable is smaller than on the rows of
the variables that have a row for the tract (\code{\link[=cacs_acs_validate]{cacs_acs_validate()}}
describes a check). Counts are combined with the coverage
weights (\code{weight_basis = "coverage"})
\deqn{c_i = \frac{a_i}{A_i},}{c_i = a_i / A_i,}
and medians and per-person values with the area shares
(\code{weight_basis = "area_mean"})
\deqn{s_i = \frac{a_i}{\sum_k a_k},}{s_i = a_i / sum(a_k),}
which sum to one.
}

\section{Totals kept for later steps}{

The \code{cacs_aggregation_carriers} attribute has the columns \code{site_id},
\code{drive_time_min}, \code{variable}, \code{estimand_family}, \code{weight_sum}, and
\code{n_tracts}, with the same values as in the result. It has four more:
\code{est_total}, the sum \eqn{\sum_i c_i X_i}{sum(c_i * X_i)} of the tract
estimates \eqn{X_i}; \code{est_mean}, the average
\eqn{\sum_i s_i X_i}{sum(s_i * X_i)}; and \code{var_total_raw} and
\code{var_mean_raw}, their variances (the squares of their standard errors).
The sum and the average are computed for every variable; \code{estimate} and
\code{moe} come from the sum for counts and from the average for medians and
per-person values.
}

\examples{
# Turn the cache off while this example runs (see ?cacs_set_cache).
old <- options(catchmentACS.cache_enabled = FALSE)

# Example data bundled with the package: the drive-time areas are circles
# with a radius of 1 km per minute, and the ACS data are made up.
library(sf)
iso <- readRDS(system.file("extdata", "legacy_2025_isochrones.rds",
                           package = "catchmentACS"))
acs <- readRDS(system.file("extdata", "sample_alabama_subset.rds",
                           package = "catchmentACS"))
iso_07 <- iso[iso$site_id == "AL_SITE_07" & iso$drive_time_min == 10, ]
agg <- cacs_intersect_weight(iso_sf = iso_07, acs_sf = acs,
                             keep_tract_audit = TRUE, verbose = FALSE)

# The coverage weight and the area share of each tract in the area (the
# three tracts have the same area, so here the area shares are the
# coverage weights divided by their sum)
tracts <- attr(agg, "cacs_tract_audit")
tracts$area_share <- tracts$int_area_m2 / sum(tracts$int_area_m2)
tracts[, c("GEOID", "area_wt", "area_share")]

# A count, a median, and a per-person value, with the weights each one uses
agg[agg$variable \%in\% c("B01003_001", "B19013_001", "B19301_001"),
    c("variable", "estimate", "moe", "weight_basis", "n_tracts")]

options(old)
}
\seealso{
\code{\link[=cacs_describe]{cacs_describe()}} prints a summary drawn from the attributes of
the result. Area weighting is explained at more length in
\code{vignette("theory-spatial-aggregation", package = "catchmentACS")},
online at
\url{https://joonho112.github.io/catchmentACS/articles/theory-spatial-aggregation.html}.

Other steps of the calculation:
\code{\link[=cacs_acs_prefetch]{cacs_acs_prefetch()}},
\code{\link[=cacs_derive_rates]{cacs_derive_rates()}},
\code{\link[=cacs_isochrone]{cacs_isochrone()}},
\code{\link[=cacs_propagate_moe]{cacs_propagate_moe()}},
\code{\link[=cacs_run]{cacs_run()}}
}
\concept{steps of the calculation}
