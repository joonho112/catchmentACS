% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-site.R
\name{cacs_plot_site_weighted}
\alias{cacs_plot_site_weighted}
\title{Map an ACS variable by tract in a drive-time area (map 3 of 4)}
\usage{
cacs_plot_site_weighted(
  site_id = NULL,
  lat = NULL,
  lon = NULL,
  site_name = NULL,
  iso_sf,
  tract_sf,
  acs_sf,
  sites_df = NULL,
  variable = "B17001_002",
  tiles = "OpenStreetMap",
  padding_km = 5,
  drive_time_min = NULL,
  variable_family = "spatial_total",
  ...
)
}
\arguments{
\item{site_id}{A string giving the \code{site_id} of the site to map, used to
find its point in \code{sites_df} and its drive-time areas in \code{iso_sf}.}

\item{lat, lon}{Single numbers giving the latitude and longitude of a
point in degrees (WGS 84), used when \code{site_id} is \code{NULL}. Both must be
given. The point is marked at that location, and the areas of every
site in \code{iso_sf} are used.}

\item{site_name}{A string giving the name of the site, compared with the
\code{site_name} column of \code{sites_df} including case, and used when
\code{site_id}, \code{lat}, and \code{lon} are \code{NULL}. It must match one row.}

\item{iso_sf}{An \code{sf} object of drive-time areas in EPSG:4326 (longitude
and latitude), such as the result of \code{\link[=cacs_isochrone]{cacs_isochrone()}}, with the
columns \code{site_id} and \code{drive_time_min}.}

\item{tract_sf}{An \code{sf} object of census tract polygons with a \code{GEOID}
column, such as the result of \code{\link[=cacs_acs_prefetch]{cacs_acs_prefetch()}}. When a tract has
several rows, as in that result (one for each variable), the first row
is used.}

\item{acs_sf}{A data frame or \code{sf} object of tract estimates in the long
form of the result of \code{\link[=cacs_acs_prefetch]{cacs_acs_prefetch()}}, with the columns \code{GEOID},
\code{variable}, \code{estimate}, and \code{moe}. It is often the same object as
\code{tract_sf}. The estimates are mapped as they are: unlike
\code{\link[=cacs_intersect_weight]{cacs_intersect_weight()}}, the map does not set Census Bureau annotation
codes, such as \code{-666666666}, to \code{NA}, and it draws a tract with two rows
for the variable twice instead of giving an error.}

\item{sites_df}{An \code{sf} object of site points in longitude and latitude
with a \code{site_id} column, or \code{NULL} (the default) for
\code{\link{cacs_alabama_sites}}. The bundled file \code{legacy_2025_isochrones.rds}
uses the same \code{site_id} values for other places, so with its areas the
default puts the point and the initial view far from them.}

\item{variable}{A string giving the ACS variable code to map, one of the
values in the \code{variable} column of \code{acs_sf}. The default is
\code{"B17001_002"}, the number of people whose income in the past 12 months
was below the poverty level.}

\item{tiles}{A string naming the background map, one of the tile
providers in \code{leaflet::providers}; the default is \code{"OpenStreetMap"}, the
standard OpenStreetMap map, which needs no API key. The map is built
offline, and the tiles are downloaded when it is displayed. The CARTO
tiles, such as \code{"CartoDB.Positron"}, need an API key, which these
functions do not send, so they show a notice asking for one.}

\item{padding_km}{A single positive number. The initial view shows at
least this many kilometers on each side of the site; the default is 5.}

\item{drive_time_min}{A single number giving the drive time, in minutes,
of the area to map, or \code{NULL} (the default) for the longest drive time
of the site in \code{iso_sf}. It must be one of the drive times in \code{iso_sf}.}

\item{variable_family}{A string giving the kind of quantity that
\code{variable} is, as in the \code{estimand_family} column of \code{\link[=cacs_run]{cacs_run()}}
results. It only chooses the ColorBrewer palette: \code{"Greens"} for
\code{"spatial_total"} (a count; the default), \code{"PuOr"} for \code{"median_proxy"}
(a median), \code{"YlOrRd"} for \code{"area_weighted_scalar_proxy"} (a
per-person value), and \code{"RdYlBu"} for \code{"derived_rate"} (a rate). \code{NA}
and unrecognized values give \code{"YlGnBu"}, and \code{"metadata_only"} gives an
error. The value is not checked against \code{variable}.}

\item{...}{Not used. Any argument given here is ignored.}
}
\value{
A \code{leaflet} map (an HTML widget).
}
\description{
Draws an interactive \code{leaflet} map of one American Community Survey (ACS)
variable over the census tracts that overlap a drive-time area of a site.
The part of each tract inside the area is shaded by the tract's estimate
as it is in \code{acs_sf}, before any weighting. The outline of the area and
the site's point are drawn on top.
}
\details{
Clicking a tract shows the values that \code{\link[=cacs_plot_site_intersection]{cacs_plot_site_intersection()}}
shows: \code{GEOID}, the coverage weight \code{area_wt} (the share of the tract's
area inside the drive-time area), and \code{intersection_km2} (the area of
that part in square kilometers), with the variable code. It also shows
the tract's estimate, its margin of error (\code{moe}, the half-width of the
90 percent confidence interval published with the estimate), and the
coverage weight times the estimate, labeled "area_wt x estimate". For a
count, the products add up to the estimate that \code{\link[=cacs_intersect_weight]{cacs_intersect_weight()}}
gives for the area, which is \code{NA} when any tract's estimate is missing.
For a median or a per-person value, that estimate is instead an average
of the tract estimates weighted by area shares (see
\code{\link[=cacs_intersect_weight]{cacs_intersect_weight()}}), and the products do not add up to it.

The estimates are grouped into color classes by natural breaks when the
classInt package is installed and there are at least five different
estimates, and by \code{pretty()} otherwise. A tract with a missing estimate,
or with no row for \code{variable} in \code{acs_sf}, is gray.

With \code{lat} and \code{lon} instead of \code{site_id}, the rows of \code{iso_sf} are not
narrowed to one site. The drive time is then chosen among the drive times
of all the sites in \code{iso_sf}, and the area of every site for that drive
time is drawn, so a map of one area needs an \code{iso_sf} that holds the rows
of one site. \code{\link[=cacs_plot_site_rates]{cacs_plot_site_rates()}} and \code{\link[=cacs_plot_site_pipeline]{cacs_plot_site_pipeline()}}
differ: they map the site in \code{sites_df} nearest to the point.
}
\examples{
if (requireNamespace("leaflet", quietly = TRUE)) {
  # Example data bundled with the package for one site in Birmingham,
  # Alabama: a 10-minute drive-time area from the OSRM routing service,
  # 2019-2023 ACS estimates for nearby tracts, and the cacs_run() result.
  fx <- readRDS(system.file("extdata", "visual_walkthrough_fixture.rds",
                            package = "catchmentACS"))
  # Median household income, a median
  cacs_plot_site_weighted(site_id = "AL_BHM_01", iso_sf = fx$iso_sf,
                          tract_sf = fx$tract_sf, acs_sf = fx$acs_sf,
                          variable = "B19013_001",
                          variable_family = "median_proxy",
                          sites_df = fx$sites_df)
}
}
\seealso{
\code{vignette("visual-walkthrough", package = "catchmentACS")}
(\url{https://joonho112.github.io/catchmentACS/articles/visual-walkthrough.html})
builds all four maps for one site, one section each.

Other maps of one site:
\code{\link[=cacs_plot_site_intersection]{cacs_plot_site_intersection()}},
\code{\link[=cacs_plot_site_isochrone]{cacs_plot_site_isochrone()}},
\code{\link[=cacs_plot_site_pipeline]{cacs_plot_site_pipeline()}},
\code{\link[=cacs_plot_site_rates]{cacs_plot_site_rates()}},
\code{\link[=format.cacs_site_plot_pipeline]{format.cacs_site_plot_pipeline()}},
\code{\link[=print.cacs_site_plot_pipeline]{print.cacs_site_plot_pipeline()}}
}
\concept{maps of one site}
