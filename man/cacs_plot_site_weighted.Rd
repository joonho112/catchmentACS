% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-site.R
\name{cacs_plot_site_weighted}
\alias{cacs_plot_site_weighted}
\title{Map a census variable across overlapping tracts (walkthrough stage 3)}
\usage{
cacs_plot_site_weighted(
  site_id = NULL,
  lat = NULL,
  lon = NULL,
  site_name = NULL,
  iso_sf,
  tract_sf,
  acs_sf,
  sites_df = NULL,
  variable = "B17001_002",
  tiles = "CartoDB.Positron",
  padding_km = 5,
  drive_time_min = NULL,
  variable_family = "spatial_total",
  ...
)
}
\arguments{
\item{site_id}{Site identifier to look up in \code{sites_df}. When omitted,
\code{sites_df} defaults to the bundled \code{cacs_alabama_sites} table.}

\item{lat, lon}{Alternative to \code{site_id}: a direct latitude/longitude
coordinate pair in WGS84 (\code{lat} in \verb{[-90, 90]}, \code{lon} in \verb{[-180, 180]}).}

\item{site_name}{Alternative to \code{site_id}: an exact match against the
\code{site_name} column of \code{sites_df}.}

\item{iso_sf}{Isochrone polygons (an \code{sf} object) such as those returned
by \code{\link[=cacs_isochrone]{cacs_isochrone()}}.}

\item{tract_sf}{Tract polygons (an \code{sf} object) with a \code{GEOID} column and
polygon geometry, typically the same ACS data passed to
\code{\link[=cacs_intersect_weight]{cacs_intersect_weight()}}.}

\item{acs_sf}{Long-format ACS data (an \code{sf} object) with columns \code{GEOID},
\code{variable}, \code{estimate}, and \code{moe}, typically the same object as
\code{tract_sf}.}

\item{sites_df}{An \code{sf} table of sites. When \code{NULL}, the bundled
\code{cacs_alabama_sites} table is used.}

\item{variable}{Single ACS variable code to map (default \code{"B17001_002"},
the count of people with income below the poverty level). It must be
present in \code{acs_sf$variable}, otherwise the function stops with an
error.}

\item{tiles}{Name of the \code{leaflet} basemap provider tiles
(default \code{"CartoDB.Positron"}).}

\item{padding_km}{Positive number giving the map padding, in kilometres,
around the site point when setting the initial view. Default \code{5}.}

\item{drive_time_min}{Single integer, or \code{NULL}, naming which drive-time
ring to intersect. When \code{NULL}, the largest available ring for the
site is used.}

\item{variable_family}{Single string naming the variable's measurement
family, used to pick the ColorBrewer palette (or \code{NA} for a neutral
fallback). Default \code{"spatial_total"}.}

\item{...}{Reserved for future keyword arguments; currently unused.}
}
\value{
A \code{leaflet} map (an HTML widget).
}
\description{
Draws a \emph{choropleth} (a map that shades areas by a data value) of one
American Community Survey (ACS) variable across the census tracts that
overlap the site's isochrone. Tract colour is chosen automatically from
a ColorBrewer palette suited to the variable type. Each tract's pop-up
reports its \code{GEOID} (Census tract identifier), the variable code, the
\code{estimate}, its margin of error (MOE, the 90 percent confidence band
published with each ACS estimate), the area weight, and the area weight
times the estimate (that tract's contribution to the site total).

As in \code{\link[=cacs_plot_site_intersection]{cacs_plot_site_intersection()}}, the tract overlaps are recomputed
on the fly from \code{iso_sf} and \code{tract_sf}, so this map needs no
pre-aggregated results.
}
\examples{
\dontrun{
iso <- readRDS(system.file("extdata", "legacy_2025_isochrones.rds",
                           package = "catchmentACS"))
acs <- readRDS(system.file("extdata", "sample_alabama_subset.rds",
                           package = "catchmentACS"))
cacs_plot_site_weighted(
  site_id  = "AL_SITE_01",
  iso_sf   = iso,
  tract_sf = acs,
  acs_sf   = acs,
  variable = "B17001_002"
)
}
}
\seealso{
\code{\link[=cacs_plot_site_pipeline]{cacs_plot_site_pipeline()}} to build all four stage maps at
once, and \code{\link[=cacs_run]{cacs_run()}} for the end-to-end aggregation these maps
illustrate.

Other visual walkthrough helpers:
\code{\link[=cacs_plot_site_intersection]{cacs_plot_site_intersection()}},
\code{\link[=cacs_plot_site_isochrone]{cacs_plot_site_isochrone()}},
\code{\link[=cacs_plot_site_pipeline]{cacs_plot_site_pipeline()}},
\code{\link[=cacs_plot_site_rates]{cacs_plot_site_rates()}},
\code{\link[=format.cacs_site_plot_pipeline]{format.cacs_site_plot_pipeline()}},
\code{\link[=print.cacs_site_plot_pipeline]{print.cacs_site_plot_pipeline()}}
}
\concept{visual walkthrough helpers}
