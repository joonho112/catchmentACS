% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/schema-diagnostics.R
\name{cacs_validate_iso}
\alias{cacs_validate_iso}
\title{Check drive-time areas and list the problems found}
\usage{
cacs_validate_iso(iso_sf)
}
\arguments{
\item{iso_sf}{An \code{sf} object of drive-time areas with one row for each
site and drive time, or any other object to check.}
}
\value{
A tibble with one row for each problem, and no rows when every
check passes. Its seven columns are strings:
\describe{
\item{\code{severity}}{Always \code{"error"}.}
\item{\code{check}}{A short name for the check, such as \code{"iso_crs"}.}
\item{\code{col}}{The column concerned, or \code{NA}.}
\item{\code{actual}, \code{expected}}{What was found, and what is required.}
\item{\code{fix_hint}, \code{example}}{A suggested fix, and a line of R code for
it, written for an object named \code{iso_sf}.}
}
}
\description{
Checks whether an object can be used as drive-time areas (isochrones) by
\code{\link[=cacs_intersect_weight]{cacs_intersect_weight()}}, or by \code{\link[=cacs_run]{cacs_run()}} through its
\code{precomputed_isochrones} argument, and returns a table with one row for
each problem found. Those two functions stop with an error at the first of
these problems. \code{\link[=cacs_run]{cacs_run()}} checks at the start only that
\code{precomputed_isochrones} is an \code{sf} object, and makes the other checks
after the download step.
}
\details{
The object must be an \code{sf} object; if it is not, that is the only problem
reported. It must have the 16 columns of the result of \code{\link[=cacs_isochrone]{cacs_isochrone()}},
with \code{ring_topology} equal to \code{"cumulative"} in every row, meaning that
each area contains the areas of the shorter drive times. An \code{isomin}
column, if present, must be 0 in every row (see
\code{\link[=cacs_rings_to_cumulative]{cacs_rings_to_cumulative()}}). The coordinate reference system must be
EPSG:4326, and the geometry type \code{POLYGON} or \code{MULTIPOLYGON}. \code{provider} and
\code{provider_requested} must be \code{"osrm"}, \code{"ors"}, \code{"mapbox"}, or \code{"r5r"},
and \code{site_id} must hold strings that are neither missing nor empty.
\code{drive_time_min} must be above 0, \code{drive_time_min} and \code{retry_count} must
be stored as integers, and \code{isochrone_empty} and \code{provider_downgrade} must
be logical.

The areas themselves are not compared, so a 5-minute area larger than the
10-minute area of the same site passes. So does a site and drive time
that appears in two rows, which \code{\link[=cacs_intersect_weight]{cacs_intersect_weight()}} turns into a row
of \code{NA} values. The function gives an error only for an \code{sf} object
without a usable geometry column, such as one made by selecting rows with
\code{[} before the sf package is loaded.

Areas made with another tool, such as the result of
\code{\link[=cacs_rings_to_cumulative]{cacs_rings_to_cumulative()}}, can be used once the missing columns hold
the values that \code{\link[=cacs_isochrone]{cacs_isochrone()}} gives an area built without a problem
(see its Value section). \code{NA} in \code{isochrone_empty} and \code{""} in
\code{failure_reason} pass the checks here, but \code{\link[=cacs_run]{cacs_run()}} treats an area as
a routing failure unless its \code{isochrone_empty} is \code{FALSE} and its
\code{failure_reason} is \code{NA}.
}
\examples{
# The drive-time areas bundled with the package: no problems, no rows
iso <- readRDS(system.file("extdata", "legacy_2025_isochrones.rds",
                           package = "catchmentACS"))
cacs_validate_iso(iso)

# Drive times stored as decimal numbers instead of integers
iso$drive_time_min <- as.numeric(iso$drive_time_min)
cacs_validate_iso(iso)
}
\seealso{
Other validation and conditions:
\code{\link[=cacs_acs_validate]{cacs_acs_validate()}},
\code{\link[=cacs_capture_conditions]{cacs_capture_conditions()}},
\code{\link[=cacs_validate_osrm_endpoint]{cacs_validate_osrm_endpoint()}},
\code{\link{catchmentACS-conditions}}
}
\concept{validation and conditions}
