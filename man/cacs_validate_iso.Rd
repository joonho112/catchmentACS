% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/schema-diagnostics.R
\name{cacs_validate_iso}
\alias{cacs_validate_iso}
\title{Preflight-check an isochrone object against the catchmentACS schema}
\usage{
cacs_validate_iso(iso_sf)
}
\arguments{
\item{iso_sf}{Object to validate, usually an \code{sf} object returned by
\code{\link[=cacs_isochrone]{cacs_isochrone()}}.}
}
\value{
A tibble with columns \code{severity}, \code{check}, \code{col}, \code{actual},
\code{expected}, \code{fix_hint}, and \code{example}. Zero rows means the object passes
every isochrone schema check.
}
\description{
Checks whether an object is usable as a catchmentACS isochrone input. An
isochrone is a polygon enclosing everywhere reachable within a given travel
time; catchmentACS represents one as an \code{sf} object (a spatial data frame).
Unlike the internal validators used inside the pipeline, this helper never
aborts on ordinary schema failures. Instead it returns a tibble with one
row per issue and a copy-paste repair example for each, so you can fix an
object interactively before passing it downstream.
}
\examples{
iso <- readRDS(system.file("extdata", "legacy_2025_isochrones.rds",
                           package = "catchmentACS"))
cacs_validate_iso(iso)
}
\seealso{
\code{\link[=cacs_isochrone]{cacs_isochrone()}} for producing a schema-valid isochrone,
\code{\link[=cacs_run]{cacs_run()}} for the pipeline that consumes it, and \code{\link[=cacs_acs_validate]{cacs_acs_validate()}}
for the ACS-side counterpart.

Other validation and conditions:
\code{\link[=cacs_acs_validate]{cacs_acs_validate()}},
\code{\link[=cacs_capture_conditions]{cacs_capture_conditions()}},
\code{\link[=cacs_validate_osrm_endpoint]{cacs_validate_osrm_endpoint()}},
\code{\link{catchmentACS-conditions}}
}
\concept{validation and conditions}
