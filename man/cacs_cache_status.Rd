% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cache.R
\name{cacs_cache_status}
\alias{cacs_cache_status}
\title{Summarize the files in the cache folder}
\usage{
cacs_cache_status()
}
\value{
A tibble with one row for each subfolder (\code{isochrone}, \code{acs},
\code{acs_test}, and \code{intersect}; see \code{\link[=cacs_clear_cache]{cacs_clear_cache()}}) and these
columns:
\describe{
\item{\code{namespace}}{The subfolder.}
\item{\code{n_entries}}{The number of saved results (\code{.rds} files).}
\item{\code{orphan_tmp}}{The number of temporary \code{.rds.tmp} files, which
are left when a write is interrupted and are never read.}
\item{\code{n_fingerprints}}{The number of checksum files (\code{.fingerprint}
files; see \code{\link[=cacs_cache_dir]{cacs_cache_dir()}}).}
\item{\code{orphan_fingerprint_tmp}}{The number of temporary
\code{.fingerprint.tmp} files, left in the same way.}
\item{\code{legacy_entries}}{The number of \code{.rds} files without a checksum
file, such as those saved by versions of the package before 0.3.0.
Such a file is deleted, and the result computed again, when a later
call looks for it.}
\item{\code{total_size_mb}}{The total size of these files and of the
GeoPackage files written by \code{cacs_acs_prefetch(write_gpkg = TRUE)},
in megabytes (\code{1024^2} bytes).}
\item{\code{oldest}, \code{newest}}{The earliest and latest modification times of
the same files, or \code{NA} when there are none. Reading a saved result
updates its modification time.}
}
Other files are not counted.
}
\description{
Counts the files in each subfolder of the cache folder returned by
\code{\link[=cacs_cache_dir]{cacs_cache_dir()}} and reports their total size and their oldest and
newest modification times. The folder is not created if it does not
exist. For a folder given in the \code{cache_dir} argument of another function,
first set the option to it, as in
\code{options(catchmentACS.cache_dir = "path/to/folder")}.
}
\examples{
cacs_cache_status()
}
\seealso{
Other cache and configuration:
\code{\link[=cacs_cache_dir]{cacs_cache_dir()}},
\code{\link[=cacs_clear_cache]{cacs_clear_cache()}},
\code{\link[=cacs_get_cache_state]{cacs_get_cache_state()}},
\code{\link[=cacs_set_cache]{cacs_set_cache()}}
}
\concept{cache and configuration}
