% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cache.R
\name{cacs_set_cache}
\alias{cacs_set_cache}
\title{Turn the cache on or off}
\usage{
cacs_set_cache(
  enabled = TRUE,
  scope = c("session", "global"),
  confirm = interactive()
)
}
\arguments{
\item{enabled}{A logical value: \code{TRUE} (the default) turns the cache on
and \code{FALSE} turns it off. \code{NA} or any other value gives an error.}

\item{scope}{A string: \code{"session"} (the default) sets the option for the
current R session, and \code{"global"} also shows the line to add to the R
startup file for later sessions (see Details).}

\item{confirm}{Not used. It is kept so that code written for earlier
versions still runs.}
}
\value{
\code{TRUE}, invisibly.
}
\description{
Turns on or off the saving and reuse of results on disk (the cache; see
\code{\link[=cacs_cache_dir]{cacs_cache_dir()}}) by \code{\link[=cacs_isochrone]{cacs_isochrone()}}, \code{\link[=cacs_acs_prefetch]{cacs_acs_prefetch()}}, and
\code{\link[=cacs_intersect_weight]{cacs_intersect_weight()}}, and so by \code{\link[=cacs_run]{cacs_run()}}. Turning the cache off
does not delete saved results.
}
\details{
\code{cacs_set_cache()} sets the option \code{catchmentACS.cache_enabled} for the
rest of the R session and changes no file. With \code{scope = "global"}, it also
shows a line to add to the R startup file (see \link[base:Startup]{Startup}) so
that later sessions start with the same setting. For \code{enabled = FALSE}, the
line sets this option. For \code{enabled = TRUE}, the line sets a cache folder
that lasts between sessions instead, because the cache is on by default
and its default folder is deleted when the session ends.

If an environment variable decides whether the cache is on (see the next
section), the message says so.

Versions 0.3.0 to 0.5.1 wrote the setting to the startup file themselves,
between the lines \verb{# catchmentACS v0.3 cache control} and
\verb{# end catchmentACS v0.3 cache control}. R still runs these lines when a
session starts, so the setting in them still applies. The package no
longer reads or removes them; delete them by hand if they are not
wanted.
}
\section{Environment variables and options}{

These environment variables and options are read at every call, so a
change takes effect at once. The rules are checked in this order, so
\code{CACS_NO_CACHE} and \code{CACS_CACHE_ENABLED} take precedence over
\code{cacs_set_cache()}:
\enumerate{
\item If the environment variable \code{CACS_NO_CACHE} is \code{"1"}, \code{"true"},
\code{"yes"}, or \code{"on"} (ignoring case), the cache is off.
\item If the environment variable \code{CACS_CACHE_ENABLED} is set and not empty,
the cache is on when its value is one of those four and off otherwise.
\item If the option \code{catchmentACS.cache_enabled} is \code{TRUE} or \code{FALSE}, it
decides. Other values are ignored.
\item Otherwise, the cache is on (the default).
}

When the cache is on, one kind of saved result can be turned off on its
own. The option \code{catchmentACS.cache_isochrone}, \code{catchmentACS.cache_acs},
or \code{catchmentACS.cache_intersect} set to \code{FALSE} turns off the cache of
\code{\link[=cacs_isochrone]{cacs_isochrone()}}, \code{\link[=cacs_acs_prefetch]{cacs_acs_prefetch()}}, or \code{\link[=cacs_intersect_weight]{cacs_intersect_weight()}}.
The environment variables \code{CACS_CACHE_ISOCHRONE}, \code{CACS_CACHE_ACS}, and
\code{CACS_CACHE_INTERSECT} do the same when set to a value other than the four
above, unless the matching option is \code{TRUE}.
}

\examples{
# Turn the cache off for this session, check the setting, and restore it
old <- options("catchmentACS.cache_enabled")
cacs_set_cache(FALSE)
cacs_get_cache_state()$enabled

# Also show the line that keeps the cache off in later sessions;
# no file is changed
cacs_set_cache(FALSE, scope = "global")
options(old)
}
\seealso{
Other cache and configuration:
\code{\link[=cacs_cache_dir]{cacs_cache_dir()}},
\code{\link[=cacs_cache_status]{cacs_cache_status()}},
\code{\link[=cacs_clear_cache]{cacs_clear_cache()}},
\code{\link[=cacs_get_cache_state]{cacs_get_cache_state()}}
}
\concept{cache and configuration}
