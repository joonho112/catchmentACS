% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cache.R
\name{cacs_set_cache}
\alias{cacs_set_cache}
\title{Enable or disable the catchmentACS cache}
\usage{
cacs_set_cache(
  enabled = TRUE,
  scope = c("session", "global"),
  confirm = interactive()
)
}
\arguments{
\item{enabled}{Logical scalar. \code{TRUE} enables cache reads and writes;
\code{FALSE} disables them.}

\item{scope}{Either \code{"session"} (current session only) or \code{"global"}
(persisted to \verb{~/.Rprofile}).}

\item{confirm}{Logical; defaults to \code{interactive()}. Global writes prompt
for confirmation unless \code{confirm = FALSE} or \code{CACS_NO_CONFIRM = "1"}.}
}
\value{
Invisibly \code{TRUE} when the requested change was applied, or \code{FALSE}
when a confirmation prompt was declined.
}
\description{
Turns the package's persistent cache on or off. With \code{scope = "session"} the
change applies to the current R session only, via an option. With
\code{scope = "global"} it writes an idempotent settings block to your
\verb{~/.Rprofile} so future sessions inherit the same setting.
}
\details{
Caching is \code{enabled = TRUE} by default. Use session scope for reversible
debugging and reproducibility checks. Use global scope only when you want
the setting to persist across sessions; that write is guarded by a
confirmation prompt unless \code{confirm = FALSE} or the environment variable
\code{CACS_NO_CONFIRM = "1"} is set.
}
\examples{
cacs_set_cache(TRUE, scope = "session")
\dontrun{
cacs_set_cache(FALSE, scope = "global")
}
}
\seealso{
\code{\link[=cacs_get_cache_state]{cacs_get_cache_state()}}, \code{\link[=cacs_clear_cache]{cacs_clear_cache()}}, \code{\link[=cacs_cache_dir]{cacs_cache_dir()}};
\code{\link[=cacs_acs_prefetch]{cacs_acs_prefetch()}} and \code{\link[=cacs_run]{cacs_run()}} honor this setting.

Other cache and configuration:
\code{\link[=cacs_cache_dir]{cacs_cache_dir()}},
\code{\link[=cacs_cache_status]{cacs_cache_status()}},
\code{\link[=cacs_clear_cache]{cacs_clear_cache()}},
\code{\link[=cacs_get_cache_state]{cacs_get_cache_state()}}
}
\concept{cache and configuration}
