% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/acs-prefetch.R
\name{cacs_acs_prefetch}
\alias{cacs_acs_prefetch}
\title{Fetch single-state ACS tract data with caching and resilient retries}
\usage{
cacs_acs_prefetch(
  state,
  year = 2023L,
  variables = NULL,
  survey = "acs5",
  geography = "tract",
  cache_dir = NULL,
  write_gpkg = FALSE,
  force_refresh = FALSE,
  drop_water_tracts = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{state}{Character(1) USPS code (\code{"AL"}) or 2-digit FIPS code
(\code{"01"}). Must be a single uppercase string; lowercase input and vectors
are rejected rather than silently coerced, so the cache key stays stable.}

\item{year}{Integer ACS five-year end year, within the release-verified
range \verb{[2009, 2024]}; default \code{2023L}.}

\item{variables}{Which ACS variables to fetch. One of: \code{NULL} (the package's
default catalogue, the dataset \code{cacs_acs_default_vars}); \code{"core"}, an alias
for \code{NULL}; \code{"extended"}, a placeholder that currently resolves to the same
set as \code{"core"}; or a character vector of explicit ACS variable codes
(each matching the pattern \code{`^B[0-9]{5}_[0-9]{3}$`}, e.g. \code{"B19013_001"}).}

\item{survey}{Character(1); \code{"acs5"} (the five-year ACS) is the only value
accepted in the current release.}

\item{geography}{Character(1); \code{"tract"} is the only value accepted in the
current release. Block-group and county geographies are planned for a
future release.}

\item{cache_dir}{Character(1) directory, or \code{NULL} (the default) to resolve
the cache root via \code{\link[=cacs_cache_dir]{cacs_cache_dir()}} and write under its \verb{acs/}
subdirectory.}

\item{write_gpkg}{Logical(1); default \code{FALSE}. When \code{TRUE}, a GeoPackage
(\code{.gpkg}) is written alongside the canonical \code{.rds} cache file for use in
QGIS, Python, or other GIS tools.}

\item{force_refresh}{Logical(1); default \code{FALSE}. When \code{TRUE}, the cache
lookup is bypassed and a live tidycensus call is forced. The fresh result
is still written back to the cache when cache writes are enabled.}

\item{drop_water_tracts}{Logical(1); default \code{TRUE}. When \code{TRUE}, any tract
whose GEOID matches the Census water / special-purpose mask
\code{`^[0-9]{2}[0-9]{3}99[0-9]{4}$`} or whose geometry has non-positive or
non-finite area is dropped right after schema validation and before
suppression handling. A message lists up to 5 dropped GEOIDs (with a
\verb{(+N more)} suffix when the list overflows). Set to \code{FALSE} to retain
water tracts (e.g. for coastline-overlap research); downstream,
\code{\link[=cacs_intersect_weight]{cacs_intersect_weight()}} then demotes its own degenerate-area abort to a
warn-and-skip path (see that function's details).}

\item{verbose}{Logical(1); default \code{TRUE}. Controls progress reporting.
Because this function makes a single \code{tidycensus::get_acs()} round-trip
per call, the automatic mode shows only a start message (\dQuote{Fetching
ACS...}) and a completion summary, with no per-step progress bar. Override
with \code{Sys.setenv(CACS_QUIET = "1")} to silence output, or
\code{options(catchmentACS.progress = "off" | "auto" | "force")}; setting
\code{"force"} upgrades the start/finish messages to a ticking progress bar.
See \code{\link[=cacs_run]{cacs_run()}} for the package-wide progress reporting policy.}
}
\value{
An \code{sf} tibble with 6 canonical columns (\code{GEOID}, \code{NAME},
\code{variable}, \code{estimate}, \code{moe}, \code{geometry}) in EPSG:4269 (NAD83), where
\code{moe} is the ACS margin of error. The result also carries a provenance
attribute recording how it was produced (state, year, variable counts,
suppression tallies, package versions, and more) and a
\code{cacs_schema_version} attribute set to \code{"1.0"}.
}
\description{
Wraps \code{tidycensus::get_acs()} to pull American Community Survey (ACS)
five-year estimates for the census tracts of a single state, then caches
the result with a content-addressed SHA-256 key so repeat calls are served
from disk. The fetch is hardened with a four-tier validation and resilience
policy: strict argument checking, tolerant handling of invalid variable
codes (skip the bad ones, abort only if all are invalid), exponential-backoff
retries on transient server errors, and carry-through of the Census Bureau's
suppression sentinels into clean \code{NA}s. Every result also carries a
provenance attribute recording exactly how it was produced. The current
release accepts a single state as a scalar USPS or FIPS code; passing a
vector of states is not yet supported.
}
\section{Cache behavior}{

ACS cache entries are guarded by SHA-256 fingerprint sidecar files. Older
\code{.rds} entries written without a sidecar are treated as a cache miss and
recomputed. A cached result with suspiciously few rows (fewer than
\code{getOption("catchmentACS.stale_threshold_rows", 1000L)}) emits a stale-cache
warning with a recovery recipe. Set the threshold to \code{0L} to disable the
heuristic, or use a state-specific option such as
\code{options(catchmentACS.stale_threshold_WY = 500L)} for small-state workflows.
A guard against single-variable false positives is controlled by
\code{options(catchmentACS.stale_min_variables = 6L)}. Inspect cache state with
\code{\link[=cacs_get_cache_state]{cacs_get_cache_state()}} and disable caching for a session with
\code{\link[=cacs_set_cache]{cacs_set_cache()}} (\code{cacs_set_cache(FALSE)}).
}

\section{Live Census access}{

Cache hits are served without a \code{CENSUS_API_KEY}. Cache misses and
\code{force_refresh = TRUE} require a live Census API key before any tidycensus
metadata or data request is attempted. This release pins five-year ACS
support to the Census years verified for it, currently \code{2009} through
\code{2024}.
}

\section{Water tracts}{

Some census tracts cover open water or other special-purpose areas and have
no usable geometry, which can break downstream spatial weighting. When
\code{drop_water_tracts = TRUE} (the default), such tracts are removed using two
independent signals, so a tract slips through only if \emph{both} the Census
GEOID convention and the geometry-area check fail to flag it:
\itemize{
\item \strong{Primary - GEOID pattern}: digits 6-7 of the GEOID equal
\code{"99"}, per the Census water / special-purpose tract convention.
\item \strong{Fallback - degenerate area}: \code{sf::st_area()} is
non-positive or non-finite, which catches geometry corruption even
when the GEOID pattern does not fire.
}
Dropped GEOIDs are reported in a single message (capped at 5 GEOIDs with a
\verb{(+N more)} suffix). Opt out via \code{drop_water_tracts = FALSE}.
}

\examples{
\dontrun{
al <- cacs_acs_prefetch(state = "AL", year = 2023, survey = "acs5",
                        geography = "tract", variables = NULL)
}
}
\seealso{
\code{\link[=cacs_run]{cacs_run()}} for the end-to-end pipeline and the package-wide
progress reporting policy; \code{\link[=cacs_intersect_weight]{cacs_intersect_weight()}} for the next step
that consumes this output; \code{\link[=cacs_acs_validate]{cacs_acs_validate()}} to check ACS data;
\code{\link[=cacs_get_cache_state]{cacs_get_cache_state()}} and \code{\link[=cacs_set_cache]{cacs_set_cache()}} for cache control.

Other core pipeline:
\code{\link[=cacs_derive_rates]{cacs_derive_rates()}},
\code{\link[=cacs_intersect_weight]{cacs_intersect_weight()}},
\code{\link[=cacs_isochrone]{cacs_isochrone()}},
\code{\link[=cacs_propagate_moe]{cacs_propagate_moe()}},
\code{\link[=cacs_run]{cacs_run()}}
}
\concept{core pipeline}
