% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/acs-prefetch.R
\name{cacs_acs_prefetch}
\alias{cacs_acs_prefetch}
\title{Download ACS estimates for the census tracts of one state}
\usage{
cacs_acs_prefetch(
  state,
  year = 2023L,
  variables = NULL,
  survey = "acs5",
  geography = "tract",
  cache_dir = NULL,
  write_gpkg = FALSE,
  force_refresh = FALSE,
  drop_water_tracts = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{state}{A string giving one state, the District of Columbia, or
Puerto Rico, as a two-letter USPS abbreviation (such as \code{"AL"}) or a
two-digit FIPS code (such as \code{"01"}). A vector of states, or a lowercase
code such as \code{"al"}, gives an error.}

\item{year}{A single whole number giving the last year of the ACS 5-year
estimates, from 2009 to 2024; the default is 2023 (the 2019-2023
estimates).}

\item{variables}{A character vector of ACS variable codes, or \code{NULL} (the
default) for \code{\link{cacs_acs_default_vars}}. Each code must have the form of
\code{"B19013_001"} (the letter B, five digits, an underscore, and three
digits); other codes, such as \code{"B01001A_001"} or \code{"C17002_001"}, give an
error. \code{"core"} and \code{"extended"} select the same variables as \code{NULL}. For
codes that are not in the ACS variable list for \code{year}, see the
"Downloading from the Census Bureau" section. The Details of
\code{\link[=cacs_intersect_weight]{cacs_intersect_weight()}} say how each code is combined; a median or
per-person value from a table other than \code{B19013}, \code{B25077}, or \code{B19301}
is added up like a count.}

\item{survey}{A string giving the survey. Only \code{"acs5"} (the default), the
ACS 5-year estimates, is accepted.}

\item{geography}{A string giving the geographic level. Only \code{"tract"} (the
default), census tracts, is accepted.}

\item{cache_dir}{A path to the cache folder, or \code{NULL} (the default) to
use \code{\link[=cacs_cache_dir]{cacs_cache_dir()}}; see the "Cache behavior" section. A folder
outside the temporary folder of the R session is tidied as described in
\code{\link[=cacs_cache_dir]{cacs_cache_dir()}}. \code{\link[=cacs_clear_cache]{cacs_clear_cache()}} clears only the folder that
\code{\link[=cacs_cache_dir]{cacs_cache_dir()}} returns.}

\item{write_gpkg}{A logical value, \code{FALSE} (the default) or \code{TRUE}. If
\code{TRUE}, a downloaded result is also written to a GeoPackage file
(\code{.gpkg}) in the \code{acs} folder of the cache folder (\code{cache_dir}, or
\code{\link[=cacs_cache_dir]{cacs_cache_dir()}} when \code{cache_dir} is \code{NULL}), for use in other GIS
software. The file is written even when the cache is turned off, but not
when a saved result is read. It is named after the cache key,
\code{attr(result, "cacs_provenance")$cache_key}, with the extension \code{.gpkg}.
By default the cache folder is inside the temporary folder of the R
session, so the file is deleted when the session ends; in a cache folder
that lasts between sessions it is deleted together with the saved result
(see \code{\link[=cacs_cache_dir]{cacs_cache_dir()}}), and \code{\link[=cacs_clear_cache]{cacs_clear_cache()}} also deletes it. To
keep a copy, write the result with \code{\link[sf:st_write]{sf::st_write()}}.}

\item{force_refresh}{A logical value. If \code{TRUE}, the data are downloaded
even when a saved result exists, and the new result replaces it unless
the cache is turned off. With \code{FALSE} (the default), a saved result is
read when there is one; see the "Cache behavior" section.}

\item{drop_water_tracts}{A logical value. If \code{TRUE} (the default), tracts
numbered 9900 or higher and tracts whose boundary has no area are
removed; if \code{FALSE}, they are kept unless they were already removed from
a saved result. See the "Water tracts" section.}

\item{verbose}{A logical value. With \code{TRUE} (the default), the function
shows a message before the download and a summary line when it
finishes, and other messages report reading a saved result, writing
files, and removing water tracts. \code{FALSE} turns off these messages;
warnings are still given. See the "Progress messages" section of
\code{\link[=cacs_run]{cacs_run()}} for how to turn the summary line off.}
}
\value{
An \code{sf} data frame with one row for each tract and variable, and
the columns \code{GEOID} (the 11-digit tract identifier: two digits for the
state, three for the county, and six for the tract), \code{NAME} (the tract
name), \code{variable} (the ACS variable code), \code{estimate}, \code{moe} (the margin
of error), and \code{geometry} (the tract boundary, in NAD83, EPSG:4269).

The attributes \code{cacs_provenance} and \code{cacs_acs_provenance} hold the
same list, a record of how the result was produced. It gives the
request: the state, the year, the survey, the geography, and the
variable codes. It counts the variables, tracts, rows, missing
estimates, and margins of error set to \code{NA}. It also holds the cache
key, the time the result was created (in UTC), and the tidycensus and
sf versions. A result read from the cache keeps the list from the
original download. The attribute \code{cacs_schema_version} is the version
label (\code{"1.0"}) of the column layout.
}
\description{
Downloads American Community Survey (ACS) 5-year estimates for the census
tracts of one state, with the tract boundaries, using
\code{tidycensus::get_acs()}. Each estimate comes with the margin of error
published with it, the half-width of its 90 percent confidence interval.
The function removes water tracts unless \code{drop_water_tracts = FALSE}, and
it returns negative margins of error as \code{NA} whatever the other arguments
are (see the "Water tracts" and "Missing estimates and margins of error"
sections). Unless the cache is turned off, the result is also saved in the
cache folder and reused by later calls with the same request (see the
"Cache behavior" section).
}
\section{Downloading from the Census Bureau}{

Reading a saved result needs neither a Census API key nor an internet
connection. The data are downloaded when no saved result is found or when
\code{force_refresh = TRUE}. A download needs a Census API key in the
\code{CENSUS_API_KEY} environment variable, which
\code{tidycensus::census_api_key()} can set; if it is missing or empty, the
function stops before any request is made. Variable codes that are not in
the ACS variable list for the year are skipped with a warning, and the
function stops if none is left. A failed download is tried up to three
times, except that an HTTP 4xx error, such as a bad request, stops the
function at once. The downloaded table is checked with the same rules as
\code{\link[=cacs_acs_validate]{cacs_acs_validate()}}.
}

\section{Water tracts}{

When \code{drop_water_tracts = TRUE} (the default), two kinds of tracts are
removed right after the download. The first are tracts numbered 9900 or
higher (a \code{GEOID} whose last six digits begin with \code{99}), which the
package treats as water or special-purpose tracts. The second are tracts
whose boundary has zero, negative, or non-finite area, such as an empty
boundary, because an area weight cannot be computed for them. A message
lists the removed \code{GEOID}s (the first five, followed by the number of
others) unless \code{verbose = FALSE}.

The same rule is applied again when a saved result is read from the cache,
but tracts removed before the result was saved stay removed even with
\code{drop_water_tracts = FALSE}. They come back only with
\code{force_refresh = TRUE} or after the saved result is deleted, for example
with \code{\link[=cacs_clear_cache]{cacs_clear_cache()}}. With
\code{drop_water_tracts = FALSE}, \code{\link[=cacs_intersect_weight]{cacs_intersect_weight()}} skips tracts with
zero area, with a warning.
}

\section{Missing estimates and margins of error}{

Negative values in the margin-of-error column, which the Census Bureau's
data API uses as codes rather than margins of error, are returned as
\code{NA}; the function does not change estimates. For example, \code{-555555555}
means that a margin of error is not appropriate because the estimate is
controlled to an independent population or housing estimate (U.S. Census
Bureau, "Notes on ACS Estimate and Annotation Values"). A warning is
given when more than 10 percent of the rows have a missing estimate, and
another when more than 10 percent have \code{-555555555} in place of a margin
of error. Step 5 in the Details of \code{\link[=cacs_intersect_weight]{cacs_intersect_weight()}} describes how
these \code{NA} values carry into the estimates for the drive-time areas.
}

\section{Cache behavior}{

By default the result is saved in the \code{acs} folder of the cache folder
(\code{cache_dir}, or \code{\link[=cacs_cache_dir]{cacs_cache_dir()}} when \code{cache_dir} is \code{NULL}), with a
small checksum file next to it. The default cache folder lasts only for
the R session; \code{\link[=cacs_cache_dir]{cacs_cache_dir()}} says how to keep saved results between
sessions and how long they are kept. A later call reads the saved result when it
asks for the same \code{state} value, year, survey, geography, and variables
(in any order) and the installed versions of R, catchmentACS, tidycensus,
tigris, and sf have not changed. Otherwise the data are downloaded again
(\code{state = "AL"} and \code{state = "01"}, for example, are saved separately).
A saved result that fails its checksum check is deleted and downloaded
again.

A saved result that is read back with fewer than 1,000 rows gives a warning
that it may be incomplete, once per session and only for copies with at
least six variables (the \code{catchmentACS.stale_min_variables} option). The
row limit is set with the option \code{catchmentACS.stale_threshold_rows} (0
turns the check off) or for one \code{state} value with an option such as
\code{catchmentACS.stale_threshold_WY}.
}

\examples{
library(sf)
# The rows of 60 census tracts around a site in Birmingham, Alabama, taken
# from the 2019-2023 estimates that cacs_acs_prefetch() downloaded for the
# state and kept in a file that comes with the package: one row for each
# tract and variable
acs_bhm <- readRDS(system.file("extdata", "visual_walkthrough_fixture.rds",
                               package = "catchmentACS"))$acs_sf
head(acs_bhm)

# Downloads the estimates for every tract in Alabama, which needs a Census
# API key.
\dontrun{
al <- cacs_acs_prefetch(state = "AL", year = 2023)
}
}
\seealso{
\code{\link[=cacs_acs_validate]{cacs_acs_validate()}} checks ACS data obtained another way, and
\code{\link[=cacs_get_cache_state]{cacs_get_cache_state()}}, \code{\link[=cacs_set_cache]{cacs_set_cache()}}, and \code{\link[=cacs_clear_cache]{cacs_clear_cache()}}
show, turn on or off, and clear the cache.

Other steps of the calculation:
\code{\link[=cacs_derive_rates]{cacs_derive_rates()}},
\code{\link[=cacs_intersect_weight]{cacs_intersect_weight()}},
\code{\link[=cacs_isochrone]{cacs_isochrone()}},
\code{\link[=cacs_propagate_moe]{cacs_propagate_moe()}},
\code{\link[=cacs_run]{cacs_run()}}
}
\concept{steps of the calculation}
