% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run.R
\name{summary.cacs_run_result}
\alias{summary.cacs_run_result}
\title{Summarize the result of \code{cacs_run()}}
\usage{
\method{summary}{cacs_run_result}(object, ..., breakdown = c("cross_site", "per_site", "both"))
}
\arguments{
\item{object}{A result of \code{\link[=cacs_run]{cacs_run()}} in the long form
(\code{output = "long"}, the default). The list-column form gives an error.}

\item{...}{Not used.}

\item{breakdown}{A string choosing the rate tables that
\code{\link[=print.cacs_run_summary]{print.cacs_run_summary()}} shows. With \code{"cross_site"} (the default), it
shows \code{rates_breakdown}, and also \code{rates_per_site} when the result has
at most \code{getOption("catchmentACS.summary_per_site_max")} sites, as in
\code{\link[=print.cacs_run_result]{print.cacs_run_result()}}. \code{"per_site"} shows only \code{rates_per_site},
and \code{"both"} shows both. The elements of the list do not depend on this
choice.}
}
\value{
A list of class \code{c("cacs_run_summary", "list")} with the elements
below, and an attribute \code{breakdown} that keeps the \code{breakdown} argument
for printing.
\describe{
\item{\code{metadata}}{The \code{cacs_run_result_metadata} attribute of
\code{object}, or \code{NULL} if it has none: the values shown in the header
of \code{\link[=print.cacs_run_result]{print.cacs_run_result()}}, the time that \code{\link[=cacs_run]{cacs_run()}} took in
seconds (\code{wall_clock_seconds}), \code{skipped_geoids}, and counts of rate
rows by \code{moe_fallback_reason} (\code{moe_fallback_summary}).}
\item{\code{n_rows}, \code{n_sites}, \code{n_variables}, \code{n_drive_times}}{The number
of rows, and the numbers of distinct values of \code{site_id},
\code{variable}, and \code{drive_time_min}.}
\item{\code{n_tracts_summary}}{The five-number summary of \code{n_tracts} from
\code{\link[stats:fivenum]{stats::fivenum()}}: minimum, lower hinge, median, upper hinge, and
maximum. Rows where \code{n_tracts} is \code{NA}, such as rate rows, are left
out.}
\item{\code{rates_breakdown}}{A tibble with one row for each of the five
rates (\code{variable}). The columns are the unweighted mean (\code{mean}) and
the standard deviation (\code{sd}) of the rate over all site and
drive-time pairs, leaving out missing values, and \code{n_NA}, the number
of pairs where the rate is missing. \code{\link[=print.cacs_run_result]{print.cacs_run_result()}} shows
the same means and standard deviations as "Top 5 rates", under the
names \code{mean_estimate} and \code{sd_estimate}. The last column of that
table, \code{n}, counts all the pairs, including those where the rate is
missing.}
\item{\code{rates_per_site}}{A tibble of the estimates of the five rates,
with one row for each site and drive time (\code{site_id},
\code{drive_time_min}).}
\item{\code{rates_per_site_moe}}{The same table with each cell a string
giving the estimate and its margin of error (the half-width of its
confidence interval), rounded to three decimals. When \code{object} has
the attribute \code{cacs_confidence_level}, the table has it as well, and
\code{\link[=cacs_summary_as_markdown]{cacs_summary_as_markdown()}} names that level in its caption.}
\item{\code{moe_fallback_rate}}{The number of rows whose \code{moe_fallback} is
\code{TRUE} (see \code{\link[=cacs_run]{cacs_run()}}), divided by the number of all rows.}
}
}
\description{
Summarizes a result of \code{\link[=cacs_run]{cacs_run()}} in a list of counts and tables of the
five rates, which \code{\link[=print.cacs_run_summary]{print.cacs_run_summary()}} prints.
}
\examples{
# A bundled cacs_run() result for a 10-minute area in Birmingham, Alabama
out <- readRDS(system.file("extdata", "visual_walkthrough_fixture.rds",
                           package = "catchmentACS"))$run_result
s <- summary(out)
s

# The rates with their margins of error
s$rates_per_site_moe
}
\seealso{
Other result summaries:
\code{\link[=as_tibble.cacs_run_result]{as_tibble.cacs_run_result()}},
\code{\link[=cacs_describe]{cacs_describe()}},
\code{\link[=cacs_summary_as_markdown]{cacs_summary_as_markdown()}},
\code{\link[=group_by.cacs_run_result]{group_by.cacs_run_result()}},
\code{\link[=print.cacs_run_result]{print.cacs_run_result()}},
\code{\link[=print.cacs_run_summary]{print.cacs_run_summary()}}
}
\concept{result summaries}
