% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-site.R
\name{cacs_plot_site_pipeline}
\alias{cacs_plot_site_pipeline}
\title{Build the full four-stage site walkthrough at once}
\usage{
cacs_plot_site_pipeline(
  site_id = NULL,
  lat = NULL,
  lon = NULL,
  site_name = NULL,
  iso_sf,
  tract_sf,
  acs_sf,
  run_result,
  sites_df = NULL,
  variable = "B17001_002",
  variable_family = "spatial_total",
  tiles = "CartoDB.Positron",
  padding_km = 5,
  drive_time_min = NULL,
  ...
)
}
\arguments{
\item{site_id}{Site identifier to look up in \code{sites_df}. When omitted,
\code{sites_df} defaults to the bundled \code{cacs_alabama_sites} table.}

\item{lat, lon}{Alternative to \code{site_id}: a direct latitude/longitude
coordinate pair in WGS84 (\code{lat} in \verb{[-90, 90]}, \code{lon} in \verb{[-180, 180]}).}

\item{site_name}{Alternative to \code{site_id}: an exact match against the
\code{site_name} column of \code{sites_df}.}

\item{iso_sf}{Isochrone polygons (an \code{sf} object) such as those returned
by \code{\link[=cacs_isochrone]{cacs_isochrone()}}.}

\item{tract_sf}{Tract polygons (an \code{sf} object) with a \code{GEOID} column and
polygon geometry, typically the same ACS data passed to
\code{\link[=cacs_intersect_weight]{cacs_intersect_weight()}}. Used by the overlap and variable maps.}

\item{acs_sf}{Long-format ACS data (an \code{sf} object) with columns \code{GEOID},
\code{variable}, \code{estimate}, and \code{moe}, typically the same object as
\code{tract_sf}. Used by the variable map.}

\item{run_result}{Output of \code{\link[=cacs_run]{cacs_run()}}. Used by the rates map.}

\item{sites_df}{An \code{sf} table of sites. When \code{NULL}, the bundled
\code{cacs_alabama_sites} table is used.}

\item{variable}{Single ACS variable code for the variable map
(default \code{"B17001_002"}, the count of people below the poverty level).}

\item{variable_family}{Single string naming the variable's measurement
family, used to pick the ColorBrewer palette for the variable map
(see \code{\link[=cacs_plot_site_weighted]{cacs_plot_site_weighted()}}). Default \code{"spatial_total"}.}

\item{tiles}{Name of the \code{leaflet} basemap provider tiles
(default \code{"CartoDB.Positron"}).}

\item{padding_km}{Positive number giving the map padding, in kilometres,
around the site point when setting the initial view. Default \code{5}.}

\item{drive_time_min}{Single integer, or \code{NULL}, naming which drive-time
ring to use for the overlap and variable maps (the isochrone and rates
maps always show all rings). When \code{NULL}, those maps use their default
(the largest available ring for the site).}

\item{...}{Reserved for future keyword arguments; passed through to each
of the four stage functions.}
}
\value{
An object of class \code{"cacs_site_plot_pipeline"}: a list with four
elements, \code{isochrone}, \code{intersection}, \code{weighted}, and \code{rates}, each a
\code{leaflet} map. The resolved \code{site_id}, \code{lat}, \code{lon}, \code{name}, and the
chosen \code{variable} are stored as attributes.
}
\description{
Builds all four stage maps for a single site and returns them together
in one object, ready to display one after another in a Quarto chunk or
report. It calls \code{\link[=cacs_plot_site_isochrone]{cacs_plot_site_isochrone()}} (stage 1, the site and its
isochrone), \code{\link[=cacs_plot_site_intersection]{cacs_plot_site_intersection()}} (stage 2, tract overlap),
\code{\link[=cacs_plot_site_weighted]{cacs_plot_site_weighted()}} (stage 3, a census variable), and
\code{\link[=cacs_plot_site_rates]{cacs_plot_site_rates()}} (stage 4, headline rates) in turn, passing the
shared styling arguments (\code{tiles}, \code{padding_km}, \code{drive_time_min}, and
\code{...}) through to each. The site is resolved once up front, so a bad
site argument fails immediately rather than partway through.
}
\section{Resolving the site}{

When you supply a direct \code{lat}/\code{lon} pair, the coordinates are matched
once to the nearest site in \code{run_result} and that \code{site_id} is reused
for all four stages. This prints one notice per call reporting the
match; if the matched site is more than 5 km away, it also warns you to
verify the coordinates.

The companion \code{\link[=print.cacs_site_plot_pipeline]{print.cacs_site_plot_pipeline()}} method lists the four
maps and how to reach them. Display any single map by typing its
accessor (for example \code{x$isochrone}) at the console.
}

\examples{
\dontrun{
iso <- readRDS(system.file("extdata", "legacy_2025_isochrones.rds",
                           package = "catchmentACS"))
acs <- readRDS(system.file("extdata", "sample_alabama_subset.rds",
                           package = "catchmentACS"))
result <- ... # cacs_run() output
pipe <- cacs_plot_site_pipeline(
  site_id    = "AL_SITE_01",
  iso_sf     = iso,
  tract_sf   = acs,
  acs_sf     = acs,
  run_result = result
)
pipe              # prints summary; doesn't auto-render
pipe$isochrone    # Stage 1 widget
pipe$rates        # Stage 4 widget
}
}
\seealso{
The four stage maps it assembles:
\code{\link[=cacs_plot_site_isochrone]{cacs_plot_site_isochrone()}}, \code{\link[=cacs_plot_site_intersection]{cacs_plot_site_intersection()}},
\code{\link[=cacs_plot_site_weighted]{cacs_plot_site_weighted()}}, and \code{\link[=cacs_plot_site_rates]{cacs_plot_site_rates()}}; and
\code{\link[=cacs_run]{cacs_run()}} for the end-to-end aggregation they illustrate.

Other visual walkthrough helpers:
\code{\link[=cacs_plot_site_intersection]{cacs_plot_site_intersection()}},
\code{\link[=cacs_plot_site_isochrone]{cacs_plot_site_isochrone()}},
\code{\link[=cacs_plot_site_rates]{cacs_plot_site_rates()}},
\code{\link[=cacs_plot_site_weighted]{cacs_plot_site_weighted()}},
\code{\link[=format.cacs_site_plot_pipeline]{format.cacs_site_plot_pipeline()}},
\code{\link[=print.cacs_site_plot_pipeline]{print.cacs_site_plot_pipeline()}}
}
\concept{visual walkthrough helpers}
