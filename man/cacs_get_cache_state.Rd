% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cache.R
\name{cacs_get_cache_state}
\alias{cacs_get_cache_state}
\title{Show the cache settings and how often saved results were used}
\usage{
cacs_get_cache_state()
}
\value{
A tibble with one row and these columns:
\describe{
\item{\code{enabled}}{Whether the cache is on (see \code{\link[=cacs_set_cache]{cacs_set_cache()}}).
This column does not show a kind of saved result that is turned off
on its own.}
\item{\code{cache_dir}}{The path of the cache folder, as returned by
\code{\link[=cacs_cache_dir]{cacs_cache_dir()}}.}
\item{\code{namespace_mode}}{\code{"test"} in test mode, which is on, for
example, while testthat runs tests and no cache folder is set with
the option \code{catchmentACS.cache_dir} or the environment variable
\code{CACS_CACHE_DIR}. American Community Survey (ACS) data are then kept
in the subfolder \code{acs_test} instead of \code{acs}. Otherwise
\code{"production"}.}
\item{\code{fingerprint_algorithm}}{The checksum method of the checksum
files, \code{"sha256"} (see \code{\link[=cacs_cache_dir]{cacs_cache_dir()}}).}
\item{\code{counters}}{A list holding a tibble with one row for each kind of
saved result (\code{isochrone}, \code{acs}, and \code{intersect}; see
\code{\link[=cacs_clear_cache]{cacs_clear_cache()}}). Its columns are \code{namespace}, \code{hit}, \code{miss},
and \code{hit_rate}, which is \code{hit / (hit + miss)} or \code{NaN} before the
first lookup. Its \code{acs} row adds up the subfolders \code{acs} and
\code{acs_test}.}
\item{\code{hits}, \code{misses}}{Lists each holding a named integer vector of
the same counts for each subfolder: \code{isochrone}, \code{acs}, \code{acs_test},
and \code{intersect}.}
\item{\code{status}}{A list holding the table returned by
\code{\link[=cacs_cache_status]{cacs_cache_status()}}.}
\item{\code{session_started_at}}{The time, in UTC, at which the package was
loaded in this session.}
}
}
\description{
Returns a one-row table with the cache settings, the number of times a
saved result was used (a hit) or not used (a miss) in this R session, and
a summary of the files in the cache folder.
}
\section{Hits and misses}{

Each time \code{\link[=cacs_isochrone]{cacs_isochrone()}}, \code{\link[=cacs_acs_prefetch]{cacs_acs_prefetch()}}, or
\code{\link[=cacs_intersect_weight]{cacs_intersect_weight()}} looks for a saved result, the lookup counts as a
hit if the saved result is used and as a miss if the result is computed
or downloaded instead. A miss happens when nothing is saved for the inputs
or the saved result fails its checksum check (see \code{\link[=cacs_cache_dir]{cacs_cache_dir()}}),
and, for the first two functions, when the cache is off.
\code{\link[=cacs_intersect_weight]{cacs_intersect_weight()}} does not look while the cache is off, and
\code{\link[=cacs_acs_prefetch]{cacs_acs_prefetch()}} does not look with \code{force_refresh = TRUE}. Lookups
in a folder given in a \code{cache_dir} argument are counted too. The counts
start when the package is loaded, and \code{\link[=cacs_clear_cache]{cacs_clear_cache()}} sets them back
to zero for the subfolders it clears.
}

\examples{
cacs_get_cache_state()

# The numbers of hits and misses for each kind of saved result
cacs_get_cache_state()$counters[[1]]
}
\seealso{
Other cache and configuration:
\code{\link[=cacs_cache_dir]{cacs_cache_dir()}},
\code{\link[=cacs_cache_status]{cacs_cache_status()}},
\code{\link[=cacs_clear_cache]{cacs_clear_cache()}},
\code{\link[=cacs_set_cache]{cacs_set_cache()}}
}
\concept{cache and configuration}
