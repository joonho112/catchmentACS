% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/isochrone-dispatch.R
\name{cacs_isochrone}
\alias{cacs_isochrone}
\title{Compute drive-time isochrones via provider dispatch}
\usage{
cacs_isochrone(
  sites,
  drive_times = c(5, 10, 15),
  provider = c("osrm", "ors", "mapbox", "r5r"),
  profile = "car",
  osrm_mode = c("demo", "docker"),
  ors_api_key = Sys.getenv("ORS_API_KEY"),
  mapbox_token = Sys.getenv("MAPBOX_TOKEN"),
  r5r_core = NULL,
  osm_snapshot_date = NULL,
  cache_dir = NULL,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{sites}{Either an \code{sf} POINT object in EPSG:4326, or a
tibble/data.frame with \code{lon}, \code{lat}, and \code{site_id} columns. A legacy
\code{point_id} column is auto-renamed to \code{site_id} with a warning.}

\item{drive_times}{Numeric vector of drive times in minutes; default
\code{c(5, 10, 15)}.}

\item{provider}{One of \code{c("osrm", "ors", "mapbox", "r5r")}; default
\code{"osrm"}. OSRM and ORS are implemented in the current release. Mapbox
and r5r are reserved provider names that fail loud with a future-release
message.}

\item{profile}{Character(1) routing profile; default \code{"car"}.}

\item{osrm_mode}{One of \code{c("demo", "docker")}; default \code{"demo"}.}

\item{ors_api_key}{Character(1) ORS API key.}

\item{mapbox_token}{Character(1) Mapbox token.}

\item{r5r_core}{An r5r core object or \code{NULL}.}

\item{osm_snapshot_date}{\code{Date}, character ISO date string, or \code{NULL}.}

\item{cache_dir}{Character(1) directory path or \code{NULL}.
Cache entries are written with \code{sha256} fingerprint sidecars.}

\item{verbose}{Logical(1); default \code{TRUE}. Drives the progress reporter.
The mode is auto-selected by a priority chain: (1) \code{verbose = FALSE}
silences output; (2) \code{Sys.getenv("CACS_QUIET") == "1"} silences output;
(3) \code{getOption("catchmentACS.progress")} of \code{"off"} silences, \code{"force"}
forces the bar, \code{"auto"} (default) falls through to the size threshold;
(4) fewer than 5 sites shows a start-and-end summary only; (5) 5 or more
sites shows a per-site progress bar with ETA and rate. See the "Progress
reporting" section of \code{\link[=cacs_run]{cacs_run()}} for the package-wide policy and how to
globally disable or force the bar regardless of batch size.}

\item{...}{Provider-specific passthrough; unknown arguments warn and are
dropped. For OSRM, \code{res} controls polygon detail. If omitted, public demo
calls resolve to \code{30L} by default to reduce HTTP 429 (rate-limit) risk;
docker and protection-disabled calls resolve to \code{70L}. Explicit \code{res}
values are respected and silent.}
}
\value{
An \code{sf} object with 16 columns in the canonical isochrone schema.
}
\description{
Given a tibble/data.frame or \code{sf} of POINT sites, computes one isochrone
(drive-time catchment polygon) per \verb{(site_id, drive_time_min)} pair via the
chosen routing provider, and returns a 16-column canonical \code{sf}.
}
\details{
Returned objects carry \code{attr(x, "cacs_isochrone_provenance")} with the cache
key, effective polygon resolution (\code{res}), effective OSRM server and profile,
request-budget diagnostics, and the cumulative ring topology, so downstream
caching can distinguish these inputs.
}
\section{Provider status}{

OSRM is the default and supports both the public demo endpoint
(\code{osrm_mode = "demo"}) and a self-hosted/local endpoint
(\code{osrm_mode = "docker"}). ORS is implemented through the optional
\code{openrouteservice} package and requires a non-empty key supplied through
\code{ors_api_key} or \code{ORS_API_KEY}.
Mapbox and r5r are intentionally retained in the provider enum for
compatibility and roadmap visibility, but they abort immediately in this
release instead of returning partial or silent placeholder results.
}

\section{OSRM res defaults and demo-budget protection}{


When \code{res} is omitted, \code{osrm_mode = "docker"} and callers who disable demo
protection use a high-resolution default of \code{70L} for more detailed polygon
geometry. The public demo endpoint is protected by resolving an omitted
\code{res} to \code{30L} when \code{osrm_mode = "demo"} and
\code{getOption("catchmentACS.osrm_demo_budget_protect", TRUE)} is \code{TRUE}.

The protected demo path emits a once-per-session message of class
\code{catchmentACS_message_demo_budget_protected}. The docker and
protection-disabled paths emit a \code{catchmentACS_message_res_default_changed}
notice and resolve to \code{70L}. To silence either notice, pass \code{res}
explicitly, e.g. \code{res = 30L}, \code{res = 50L}, or \code{res = 70L}.
}

\section{Per-urbanicity expected divergence}{


Raw isochrone polygon area is sensitive to road-network density, routing
engine snapshots, and polygon-resolution choices. In an Alabama validation
replay, the practical area-difference bands were approximately urban
+/-5 percent, mid-density +/-15 percent, and rural +/-30 percent under
ordinary conditions. A sparse rural anchor exceeded that wide typical band
at about 55 percent area difference versus the archive run, while its
tract-membership Jaccard index was 1.000.

For policy aggregation, tract membership and downstream weighted estimates
are the trusted invariants; raw polygon area is a routing/snapshot diagnostic.
When comparing to older archive runs, interpret area divergence by site
context: use tight bands for urban sites, wider bands for mid-density sites,
and inspect rural outliers with a tract-membership/Jaccard check before
treating an area difference as an aggregation bug. See
\code{vignette("getting-started", package = "catchmentACS")} and
\code{vignette("porting-v01-to-v03", package = "catchmentACS")} for examples.
}

\section{OSRM performance note}{


The upstream \code{osrm} package computes isochrones by querying a travel-time
table over a square grid of \code{res * res} destinations. With the public
\code{routing.openstreetmap.de} demo endpoint, that package chunks requests in
groups of 75 and sleeps one second per full chunk. At \code{res = 70L}, this
implies 4,900 grid destinations and about 65 seconds of forced sleep per
site before ordinary network and geometry overhead. The public-demo
omitted default of \code{30L} substantially lowers that table budget.

\code{osrm_mode = "docker"} now resolves to a local OSRM server
(\verb{http://0.0.0.0:5000/} by default, override with
\code{options(catchmentACS.osrm_docker_server = "...")} or \code{osrm.server = ...}).
Custom/self-hosted endpoints use the fast \code{osrm} package path with no
public-demo forced sleep. The effective server/profile participate in the
isochrone cache key.
}

\examples{
\dontrun{
data("cacs_alabama_sites")
iso <- cacs_isochrone(
  sites       = cacs_alabama_sites[1:3, ],
  drive_times = c(5, 10),
  provider    = "osrm",
  osrm_mode   = "demo"
)
}
}
\seealso{
\code{\link[=cacs_run]{cacs_run()}} for the orchestrator that drives this step and the
package-wide progress reporting policy, \code{\link[=cacs_intersect_weight]{cacs_intersect_weight()}} for the
downstream step this output feeds, and \code{\link[=cacs_acs_validate]{cacs_acs_validate()}}.

Other core pipeline:
\code{\link[=cacs_acs_prefetch]{cacs_acs_prefetch()}},
\code{\link[=cacs_derive_rates]{cacs_derive_rates()}},
\code{\link[=cacs_intersect_weight]{cacs_intersect_weight()}},
\code{\link[=cacs_propagate_moe]{cacs_propagate_moe()}},
\code{\link[=cacs_run]{cacs_run()}}
}
\concept{core pipeline}
