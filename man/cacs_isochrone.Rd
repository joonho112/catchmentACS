% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/isochrone-dispatch.R
\name{cacs_isochrone}
\alias{cacs_isochrone}
\title{Build drive-time areas around sites with a routing service}
\usage{
cacs_isochrone(
  sites,
  drive_times = c(5, 10, 15),
  provider = c("osrm", "ors", "mapbox", "r5r"),
  profile = "car",
  osrm_mode = c("demo", "docker"),
  ors_api_key = Sys.getenv("ORS_API_KEY"),
  mapbox_token = Sys.getenv("MAPBOX_TOKEN"),
  r5r_core = NULL,
  osm_snapshot_date = NULL,
  cache_dir = NULL,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{sites}{An \code{sf} object of points in EPSG:4326, or a data frame with
numeric columns \code{lon} and \code{lat} (longitude and latitude). An \code{sf} object
in another coordinate reference system, including NAD83 (EPSG:4269),
gives an error. Either form needs a column \code{site_id} with a different
value for each site; a numeric \code{site_id} becomes a string in the result.
Without a \code{site_id} column, a column \code{point_id} is used as \code{site_id},
with a warning.}

\item{drive_times}{A numeric vector of drive times in minutes: at most six
values, each a whole number from 1 to 60; the default is \code{c(5, 10, 15)}.
The values can be in any order, and a repeated value is used once.}

\item{provider}{A string giving the routing service: \code{"osrm"} (the
default) for OSRM or \code{"ors"} for openrouteservice; see the
"Provider status" section. \code{"mapbox"} and \code{"r5r"} are also accepted, but
they are not implemented yet; using them gives an error of class
\code{catchmentACS_error_credential}, after the other arguments are checked
and before any request is sent.}

\item{profile}{A string giving the routing profile, such as \code{"car"} (the
default). With OSRM, the public demo server offers \code{"car"}, \code{"bike"},
and \code{"foot"}; with a profile that is not available, routing fails for
every site and the error is recorded in \code{failure_reason}. With
openrouteservice, the accepted values are the openrouteservice profiles
\code{"driving-car"}, \code{"driving-hgv"}, \code{"cycling-regular"}, \code{"cycling-road"},
\code{"cycling-mountain"}, \code{"cycling-electric"}, \code{"foot-walking"},
\code{"foot-hiking"}, and \code{"wheelchair"}, and the short names \code{"car"},
\code{"hgv"}, \code{"bike"} (for \code{"cycling-regular"}), \code{"foot"}, and \code{"walk"} (both
for \code{"foot-walking"}); any other value stops the function with the R
error "subscript out of bounds".}

\item{osrm_mode}{A string giving where OSRM requests are sent: \code{"demo"}
(the default) for the public OSRM demo server or \code{"docker"} for a local
OSRM server. It also sets the default \code{res}; see the OSRM sections
below.}

\item{ors_api_key}{A string giving the openrouteservice API key, by
default the value of the \code{ORS_API_KEY} environment variable. It is used
only with \code{provider = "ors"}; see the "Provider status" section.}

\item{mapbox_token}{A string, by default the value of the \code{MAPBOX_TOKEN}
environment variable. It is not used (see \code{provider}), but a value that
is not a single string, such as \code{NULL}, gives an error.}

\item{r5r_core}{An r5r core object, or \code{NULL} (the default). It is not
used (see \code{provider}), but its class is checked.}

\item{osm_snapshot_date}{A date to record as the date of the OpenStreetMap
data used by the routing service: a \code{Date} object or a string in the
form year-month-day, such as \code{"2025-04-01"}, or \code{NULL} (the default).
The date is only recorded in the result; it is not sent to the routing
service and does not change the areas. A call with a different date
does not reuse a saved result.}

\item{cache_dir}{A path to the cache folder, or \code{NULL} (the default) to use
\code{\link[=cacs_cache_dir]{cacs_cache_dir()}}. A folder outside the temporary folder of the R
session is tidied as described in \code{\link[=cacs_cache_dir]{cacs_cache_dir()}}.}

\item{verbose}{A logical value. With \code{TRUE} (the default), the function
shows a summary line when the step finishes and, with five or more
sites, a line as each site finishes. See the "Progress messages"
section of \code{\link[=cacs_run]{cacs_run()}} for how to turn them off or show the lines for
any number of sites. The function shows the message that a saved result
is returned, and the notices about the default \code{res}, even when
\code{verbose} is \code{FALSE}.}

\item{...}{Additional named arguments, which depend on \code{provider}. With
OSRM, the accepted names are \code{res}, which sets how detailed the areas are
(see the OSRM sections below); \code{osrm.server}, the address of an OSRM
server to use instead of the one chosen by \code{osrm_mode}; and
\code{osrm.profile} or \code{osrm.profile_name}, a profile name to use in the
requests instead of \code{profile}. With openrouteservice, the accepted names
are \code{attributes}, \code{area_units}, and \code{smoothing}, which are passed on to
\code{openrouteservice::ors_isochrones()}. Other arguments give a warning and
are not used.}
}
\value{
An \code{sf} tibble with one row for each site and drive time, in the
order of \code{sites} and then by increasing drive time, and 16 columns:
\itemize{
\item \code{site_id}: the site identifier, converted to a string.
\item \code{drive_time_min}: the drive time in minutes.
\item \code{geometry}: the drive-time area, of geometry type \code{POLYGON} or
\code{MULTIPOLYGON}, in longitude and latitude (EPSG:4326); empty when there
is no area.
\item \code{provider}: the routing service used, \code{"osrm"} or \code{"ors"}.
\item \code{profile}: the routing profile, such as \code{"car"}.
\item \code{osm_snapshot_date}: the date given in \code{osm_snapshot_date}, as a
string, or \code{"unknown"}.
\item \code{routing_engine_version}: \code{"osrm-pkg/"} or \code{"openrouteservice-pkg/"}
followed by the version of the R package that sent the requests (not
the version of the routing server).
\item \code{polygon_simplification_tolerance}: always \code{NA}.
\item \code{provider_requested}: the service asked for, always the same as
\code{provider}.
\item \code{provider_downgrade}: whether another service was used instead,
always \code{FALSE}.
\item \code{generated_at}: the time the areas were built; a result read from the
cache keeps the time of the original call.
\item \code{isochrone_empty}: \code{TRUE} when the row has no area, because routing
failed or because the service returned no area for that drive time
(with OSRM, for example, when the site is too far from the road
network).
\item \code{osm_snapshot_status}: \code{"user_supplied"} when \code{osm_snapshot_date} was
given, otherwise \code{"unknown_best_effort"}.
\item \code{failure_reason}: \code{NA} when routing succeeded, otherwise the error
message from the last attempt.
\item \code{retry_count}: the number of attempts made for the site, counting the
first (1 to 3).
\item \code{ring_topology}: always \code{"cumulative"}, meaning that each area
includes the areas of the shorter drive times.
}

The attributes \code{cacs_isochrone_provenance} and \code{cacs_provenance} hold
the same list, a record of how the result was produced: the cache key,
\code{provider}, \code{profile}, \code{osrm_mode}, the snapshot date and status,
\code{ring_topology}, and, for OSRM, the \code{res} value, the OSRM server and
profile, and an estimate of the requests per site
(\code{osrm_request_budget}). The
attribute \code{cacs_res_param} holds the \code{res} value (\code{NA} for
openrouteservice). Rows selected from the result keep these attributes,
including the cache key that \code{\link[=cacs_intersect_weight]{cacs_intersect_weight()}} uses to recognize
the areas in its own cache.
}
\description{
Builds a drive-time area around each site for each drive time, using a
routing service: the Open Source Routing Machine (OSRM, the default) or
openrouteservice. Each area, also called an isochrone, covers the places
that the routing service finds reachable from the site within that many
minutes, so the 10-minute area includes the 5-minute area.
}
\details{
Routing for a site is tried up to three times, and not again after an
HTTP 4xx error such as a bad request. A site for which routing fails keeps
its rows, with an empty geometry and the error message in
\code{failure_reason}; warnings report the number of rows that failed or have
no area. If the routing service answers that its request limit has been
reached (HTTP status 429), the function stops with an error as soon as
that answer comes, sends no requests for the remaining sites, and returns
no result. It does the same when the service refuses access (HTTP status
401 or 403), for example because an API key is not accepted.

The requests have no time limit of their own: a server that accepts the
connection but does not answer makes the function wait, and a server that
cannot be reached is tried three times for each site before the site is
reported as failed. \code{\link[=cacs_validate_osrm_endpoint]{cacs_validate_osrm_endpoint()}} checks an OSRM server
with a time limit and can be called first.

Unless the cache is turned off with \code{\link[=cacs_set_cache]{cacs_set_cache()}}, the result is
saved in the cache folder (\code{cache_dir}, or \code{\link[=cacs_cache_dir]{cacs_cache_dir()}}), which by
default lasts only for the R session. A later call with the same arguments
(other than \code{verbose}) and option settings returns the saved result
without contacting the routing service, as long as the installed versions
of sf, PROJ, and GEOS have not changed. A result is not saved when routing
failed for a site because the service did not answer, timed out, or
answered with an HTTP 5xx error, since a later call may succeed; a saved
result with such a failure is deleted, and its sites are routed again. A
site that the service refused with another HTTP 4xx error, such as a bad
request, is saved with its \code{failure_reason}, and a warning is given each
time the saved result is used. \code{\link[=cacs_clear_cache]{cacs_clear_cache()}} with
\code{namespace = "isochrone"} removes only the results saved in the folder
returned by \code{\link[=cacs_cache_dir]{cacs_cache_dir()}}, whose help page describes the cache
folder, how long saved results are kept, and how they are checked.
}
\section{Provider status}{

\code{provider = "osrm"} (the default) uses OSRM through the osrm package and
needs no API key; \code{osrm_mode} chooses the server (see the OSRM sections
below). \code{provider = "ors"} uses openrouteservice through the
openrouteservice package and needs an API key, given in \code{ors_api_key}; if
the key is empty, the function stops with an error of class
\code{catchmentACS_error_credential} before any request is sent. The
openrouteservice route has been checked only with simulated responses
from openrouteservice, not with the service itself. The two services
compute the areas in different ways, so the area for the same site and
drive time can differ between them.
}

\section{OSRM servers}{

With \code{osrm_mode = "demo"}, the requests go to the public OSRM demo server
at \verb{https://routing.openstreetmap.de/}; with \code{osrm_mode = "docker"}, they
go to \verb{http://0.0.0.0:5000/}, or to the address set in the option
\code{catchmentACS.osrm_docker_server}. An address given in \code{osrm.server} (see
\code{...}) is used instead with either mode. With \code{osrm_mode = "demo"}, the
osrm package's own option \code{osrm.server} is also used. The osrm package
sets that option to the demo server when it is loaded, for example by
\code{library(osrm)}, replacing a value set earlier; a value set before a call
to this function is kept, even when the call loads osrm.
}

\section{OSRM grid resolution}{

The osrm package draws the areas of a site from travel times to a grid of
\code{res} by \code{res} points around it, sized for the longest drive time, so the
areas for shorter drive times rest on fewer points. A larger \code{res} gives
more detailed areas but needs more requests and more time. On the demo
server, the osrm package waits one second after every 75 grid points that
it sends, which comes to about 10 seconds for each site at \code{res = 30L}
and about a minute at \code{res = 70L}. There is no such wait with other
servers.

If \code{res} is not given, it is \code{70L} with \code{osrm_mode = "docker"}. The
public demo server is protected by resolving \code{res} to \code{30L} with
\code{osrm_mode = "demo"}. With the lower value, fewer requests are sent for
each site, so the server is less likely to answer that its request limit
has been reached (HTTP status 429). With
\code{options(catchmentACS.osrm_demo_budget_protect = FALSE)},
\code{res} is \code{70L} with either mode. A message reports the value used, once
per R session; its class is
\code{catchmentACS_message_demo_budget_protected} for \code{30L} and
\code{catchmentACS_message_res_default_changed} for \code{70L}. When \code{res} is
given, such as \code{res = 30L}, \code{res = 50L}, or \code{res = 70L}, that value is
used and neither message is shown.

From version 5.0.0, the osrm package may show the message "'res' is
deprecated, use 'n' instead." for each site. The \code{res} value is still
used, and \code{n} is not accepted by \code{cacs_isochrone()}.

Areas built with another server, other OpenStreetMap data, or another
\code{res} can differ, and so can the estimates computed from them.
}

\examples{
# The 10-minute area that cacs_isochrone() built for one site in
# Birmingham, Alabama, with the public OSRM demo server, kept in a file that
# comes with the package
iso_bhm <- readRDS(system.file("extdata", "visual_walkthrough_fixture.rds",
                               package = "catchmentACS"))$iso_sf
sf::st_drop_geometry(iso_bhm)[, c("site_id", "drive_time_min", "provider",
                                  "isochrone_empty", "failure_reason")]

# A table with no rows: the area has the form that the package expects
cacs_validate_iso(iso_bhm)

# These calls send several dozen requests to the public OSRM demo server,
# which limits how many it accepts.
\dontrun{
library(sf)
# The site at the center of the drive-time areas for AL_SITE_07 that come
# with the package (used in the examples of cacs_run() and others)
site_07 <- data.frame(site_id = "AL_SITE_07", lon = -85.365, lat = 31.655)

# 5-, 10-, and 15-minute areas from the public OSRM demo server
iso <- cacs_isochrone(site_07, drive_times = c(5, 10, 15))
st_drop_geometry(iso)[, c("site_id", "drive_time_min", "isochrone_empty")]

# A finer grid gives more detailed areas but takes longer
iso_fine <- cacs_isochrone(site_07, drive_times = c(5, 10, 15), res = 50L)
}
}
\seealso{
\code{\link[=cacs_validate_osrm_endpoint]{cacs_validate_osrm_endpoint()}} checks whether an OSRM server is
accepting requests, and \code{\link[=cacs_validate_iso]{cacs_validate_iso()}} checks whether drive-time
areas made with other tools are in the form that the package expects.
The routing services, and how to use a local OSRM server, are described
in \code{vignette("providers", package = "catchmentACS")}
(\url{https://joonho112.github.io/catchmentACS/articles/providers.html}).

Other steps of the calculation:
\code{\link[=cacs_acs_prefetch]{cacs_acs_prefetch()}},
\code{\link[=cacs_derive_rates]{cacs_derive_rates()}},
\code{\link[=cacs_intersect_weight]{cacs_intersect_weight()}},
\code{\link[=cacs_propagate_moe]{cacs_propagate_moe()}},
\code{\link[=cacs_run]{cacs_run()}}
}
\concept{steps of the calculation}
